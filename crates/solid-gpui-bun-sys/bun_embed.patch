diff --git a/scripts/build/bun.ts b/scripts/build/bun.ts
index ae2fdef..0787a08 100644
--- a/scripts/build/bun.ts
+++ b/scripts/build/bun.ts
@@ -19,6 +19,9 @@
  *   - "link-only": link pre-built artifacts (CI downstream)
  *   - "rust-and-link": cargo + link; downloads cpp-only's archive (CI)
  *   - "archive-link": full build on one agent, linked from the cpp-only-style archive; uploads it + libbun_rust.a (CI)
+ *   - "embed-native": codegen + deps + every C/C++ object for a host-owned
+ *     static link. No Rust staticlib, no CLI entry point, no PE resources;
+ *     emits embed-native.json (see scripts/build/embed-native.ts)
  *
  * The split modes are for CI where C++ and Rust build in parallel on
  * separate machines. rust-and-link folds the rust + link steps onto one
@@ -36,6 +39,7 @@ import { generateDepVersionsHeader } from "./depVersionsHeader.ts";
 import { allDeps } from "./deps/index.ts";
 import { lolhtml } from "./deps/lolhtml.ts";
 import { rustArgon2 } from "./deps/rust-argon2.ts";
+import { emitEmbedNativeProduct } from "./embed-native.ts";
 import { assert } from "./error.ts";
 import { bunIncludes, computeFlags, extraFlagsFor, linkDepends, linkerMapOutputs } from "./flags.ts";
 import { writeIfChanged } from "./fs.ts";
@@ -191,8 +195,11 @@ export function emitBun(n: Ninja, cfg: Config, sources: Sources): BunOutput {
   // bundle-modules / generate-jssink edges), so the codegen output set
   // is forwarded as implicit inputs to order it first.
   //
-  // cpp-only: skip rust entirely (runs on a separate CI machine).
-  let rustObjects: string[] = [];
+  // Vendored Rust path deps must be on disk before ANY cargo invocation
+  // resolves the workspace — including the embedding host's, which consumes
+  // this checkout as a path dependency from *outside* this graph. Resolve them
+  // for every mode that reaches cargo, directly or through the host.
+  let vendorStamps: string[] = [];
   if (cfg.mode !== "cpp-only") {
     // lol-html is a direct path dep of `bun_runtime`/`bun_bundler`
     // (`lol_html = { path = "vendor/lolhtml" }` in the workspace Cargo.toml),
@@ -205,11 +212,18 @@ export function emitBun(n: Ninja, cfg: Config, sources: Sources): BunOutput {
     const rustArgon2Dep = resolveDep(n, cfg, rustArgon2, depsByName);
     assert(rustArgon2Dep !== null, "rust-argon2 resolveDep returned null — should never be skipped");
     depsByName.set(rustArgon2.name, rustArgon2Dep);
+    vendorStamps = [...lolhtmlDep.outputs, ...rustArgon2Dep.outputs];
+  }
+  // cpp-only: skip rust entirely (runs on a separate CI machine). embed-native:
+  // the host crate graph compiles bun's Rust crates itself, as an rlib in its
+  // own graph — no second staticlib, no second `std`, no second allocator.
+  let rustObjects: string[] = [];
+  if (cfg.mode !== "cpp-only" && cfg.mode !== "embed-native") {
     rustObjects = emitRust(n, cfg, {
       codegenInputs: codegen.rustInputs,
       codegenOrderOnly: codegen.rustOrderOnly,
       rustSources: sources.rust,
-      vendorStamps: [...lolhtmlDep.outputs, ...rustArgon2Dep.outputs],
+      vendorStamps,
     });
   }
 
@@ -441,6 +455,25 @@ export function emitBun(n: Ninja, cfg: Config, sources: Sources): BunOutput {
   // cfg.archiveDeps they live in depLibs as .a files instead.
   const allObjects = [...cxxObjects, ...cObjects, ...depObjects];
 
+  // ─── Step 6: embed-native → static embedding product (returns here) ───
+  // The host application owns the final link, so stop here with the object set
+  // and the link policy rather than continuing into the CLI's archive / link /
+  // post-link steps. The manifest this emits is the native product's whole
+  // contract; nothing else about bun-debug's build leaks into the host link.
+  if (cfg.mode === "embed-native") {
+    return emitEmbedNativeProduct(n, cfg, {
+      codegen,
+      deps,
+      objects: allObjects,
+      archives: depLibs,
+      checks: depChecks,
+      ldflags: [...flags.ldflags, ...systemLibs(cfg)],
+      shims: emitShims(n, cfg),
+      rustSources: sources.rust,
+      vendorStamps,
+    });
+  }
+
   // ─── Step 6: cpp-only / archive-link → archive (cpp-only returns here) ───
   // CI's build-cpp step: archive all .o into libbun.a, stop. The sibling
   // build-rust step produces libbun_rust.a independently; build-bun
diff --git a/scripts/build/config.ts b/scripts/build/config.ts
--- a/scripts/build/config.ts
+++ b/scripts/build/config.ts
@@ -21,7 +21,17 @@ export type OS = "linux" | "darwin" | "windows" | "freebsd";
 export type Arch = "x64" | "aarch64";
 export type Abi = "gnu" | "musl" | "android";
 export type BuildType = "Debug" | "Release" | "RelWithDebInfo" | "MinSizeRel";
-export type BuildMode = "full" | "cpp-only" | "rust-only" | "link-only" | "rust-and-link" | "archive-link";
+export type BuildMode =
+  | "full"
+  | "cpp-only"
+  | "rust-only"
+  | "link-only"
+  | "rust-and-link"
+  | "archive-link"
+  // Static embedding product: the shared codegen/dependency/C++ phases only.
+  // The host application — a separate Rust crate graph that consumes `bun_bin`
+  // as an rlib — owns the final link. See scripts/build/embed-native.ts.
+  | "embed-native";
 export type WebKitMode = "prebuilt" | "local";
 
 /**
@@ -305,9 +315,9 @@
   /**
-   * Windows cross-compile only: root of an xwin-style splat of the MSVC
+   * Windows target sysroot: root of an xwin-style splat of the MSVC
    * CRT/STL + Windows SDK laid out like a Visual Studio install
    * (`VC/Tools/MSVC/<ver>`, `Windows Kits/10`). Passed to clang-cl as
-   * `/winsysroot` and to lld-link as `/winsysroot:` — the cross equivalent
-   * of the INCLUDE/LIB env a VS dev shell provides on a Windows host.
-   * undefined on native Windows builds (VS dev shell supplies the SDK).
+   * `/winsysroot` and to lld-link as `/winsysroot:`. Explicit selection also
+   * applies on Windows so a native build can match prebuilt WebKit's STL ABI.
+   * Undefined on native Windows only when no sysroot was requested.
    */
   winsysroot: string | undefined;
@@ -391,3 +401,3 @@
   osxDeploymentTarget?: string;
-  /** Windows sysroot (xwin splat, VS layout). Only used when cross-compiling for os=windows. */
+  /** Windows target sysroot (xwin splat, VS layout), including native builds. */
   winsysroot?: string;
@@ -1056,18 +1066,16 @@
   // ─── Cross-compilation (Windows) ───
   // Same pattern as Android/FreeBSD, with the MSVC spin: the host LLVM's
   // clang-cl/lld-link/llvm-lib/llvm-rc are used (tools.ts picks them by
   // target), and the "sysroot" is an xwin splat of the MSVC CRT/STL +
   // Windows SDK in Visual Studio layout, passed via /winsysroot instead of
-  // --sysroot. Building ON Windows needs none of this — the VS dev shell
-  // provides INCLUDE/LIB.
-  let winsysroot: string | undefined;
+  // --sysroot. Explicit selection must also override the native VS toolset;
+  // newer STL headers can change the prebuilt WebKit's C++ calling conventions.
+  let winsysroot =
+    windows && partial.winsysroot !== undefined
+      ? resolve(cwd, partial.winsysroot)
+      : undefined;
   if (windows && host.os !== "windows") {
-    winsysroot =
-      partial.winsysroot !== undefined
-        ? isAbsolute(partial.winsysroot)
-          ? partial.winsysroot
-          : resolve(cwd, partial.winsysroot)
-        : detectWindowsSysroot();
+    winsysroot ??= detectWindowsSysroot();
     if (winsysroot === undefined) {
       if (ci || buildkite) {
         // CI always fetches its own sysroot into the per-build cache (see
@@ -1116,6 +1124,9 @@
   let osxSysroot: string | undefined;
   if (darwin && host.os === "darwin") {
     ({ osxDeploymentTarget, osxSysroot } = detectMacosSdk(ci));
+    if (partial.macosSdk !== undefined) {
+      osxSysroot = resolveMacosSdkPath(partial.macosSdk, cacheDir, cwd);
+    }
     if (partial.osxDeploymentTarget !== undefined) osxDeploymentTarget = partial.osxDeploymentTarget;
   }
 
diff --git a/scripts/build/configure.ts b/scripts/build/configure.ts
--- a/scripts/build/configure.ts
+++ b/scripts/build/configure.ts
@@ -278,11 +278,11 @@
-  // Windows cross-compile: make sure the MSVC CRT + Windows SDK splat is
+  // Explicit Windows sysroot: make sure the MSVC CRT + Windows SDK splat is
   // usable BEFORE the graph is emitted — emitBun() enumerates its include
   // dirs (llvm-rc's /I flags) at configure time, so the sysroot must exist
   // by then, not just before ninja runs. CI fetches a missing sysroot into
   // the per-build cache; local builds require a provisioned one (the fetch
   // would be a surprise multi-GB download) and only get the case-alias
   // fixup + completeness check.
-  if (cfg.windows && cfg.host.os !== "windows") {
+  if (cfg.windows && cfg.winsysroot !== undefined) {
     await ensureWindowsSysroot(cfg);
     mark("ensureWindowsSysroot");
   }
@@ -298,8 +298,14 @@ export async function configure(input: ConfigureInput): Promise<ConfigureResult>
   // perl script from JSC. If perl is missing, codegen fails cryptically.
   // Check here so the error is at configure time with a clear hint.
   // rust-only/link-only don't run LUT codegen — skip the check so split-CI
-  // steps don't require perl on the rust cross-compile box.
-  if (cfg.mode === "full" || cfg.mode === "cpp-only" || cfg.mode === "archive-link") {
+  // steps don't require perl on the rust cross-compile box. embed-native
+  // compiles the same C/C++ set as `full`, so it needs perl too.
+  if (
+    cfg.mode === "full" ||
+    cfg.mode === "cpp-only" ||
+    cfg.mode === "archive-link" ||
+    cfg.mode === "embed-native"
+  ) {
     if (findSystemTool("perl") === undefined) {
       throw new BuildError("perl not found in PATH", {
         hint: "LUT codegen (create-hash-table.ts) needs perl. Install it: apt install perl / brew install perl",
diff --git a/scripts/build/rust.ts b/scripts/build/rust.ts
index 5b7872b..fd11d16 100644
--- a/scripts/build/rust.ts
+++ b/scripts/build/rust.ts
@@ -762,6 +762,130 @@ export function cargoBuildInvocation(cfg: Config): CargoInvocation {
   return { args, env, targetDir, triple };
 }
 
+/**
+ * Emit the Windows `.bin/` shim PE build edge and return its stamps.
+ *
+ * The shim is a freestanding release PE that `bun_install` reaches through
+ * `include_bytes!`, so `bun install` can write a real launcher into
+ * `node_modules/.bin/`. Without this edge the 0-byte placeholder that
+ * `src/install/build.rs` creates for a bare `cargo check` is what ships, and
+ * `embedded_executable_data()` trips its non-empty assertion at runtime.
+ *
+ * Split out of `emitRust` because the static embedding product
+ * (scripts/build/embed-native.ts) links no Rust staticlib but *does* compile
+ * `bun_install` — through the host crate graph — so the real PE has to be in
+ * the shared source-tree path before that graph is built. Same env as the main
+ * build (toolchain forwarding, CARGO_HOME) but no codegen dep: the shim crate's
+ * graph is bun_core/bun_sys/bun_string only.
+ *
+ * Returns the stamps to depend on — empty off Windows.
+ */
+export function emitWindowsShim(n: Ninja, cfg: Config, inputs: RustBuildInputs): string[] {
+  if (!cfg.windows) return [];
+  const { env, targetDir, triple } = cargoBuildInvocation(cfg);
+  const hostWin = cfg.host.os === "windows";
+  const shimDest = windowsShimDestPath(cfg);
+  // Always `--profile shim` (workspace `[profile.shim]`: panic=abort,
+  // opt-level=z, lto, codegen-units=1, strip) regardless of bun's own
+  // profile — a debug bun should still write release shims (matches Zig's
+  // unconditional `.ReleaseFast`).
+  //
+  // `-Zbuild-std=core,compiler_builtins` rebuilds the sysroot for the
+  // freestanding `#![no_std]` crate so LTO can inline across `core`;
+  // `panic_immediate_abort` makes every `panic!`/`unreachable!`/`assert!`
+  // (incl. those buried in `core::fmt`, slice indexing, `Option::unwrap`)
+  // compile to a bare `ud2`/`brk` with no `core::fmt::Arguments` payload —
+  // that machinery is otherwise the bulk of `.text`. Nightly + `rust-src`
+  // are guaranteed by `rust-toolchain.toml`.
+  const shimArgs: string[] = [
+    "-p",
+    "bun_shim_impl",
+    "--bin",
+    "bun_shim_impl",
+    "--features",
+    "shim_standalone",
+    "--target-dir",
+    targetDir,
+    "--target",
+    triple,
+    "--profile",
+    "shim",
+    "--locked",
+    "-Zbuild-std=core,compiler_builtins",
+    "-Zbuild-std-features=compiler-builtins-mem",
+  ];
+  const shimSrc = resolve(targetDir, triple, "shim", "bun_shim_impl.exe");
+  // Same env minus the main build's CARGO_ENCODED_RUSTFLAGS — the shim has
+  // its own panic strategy (abort) so `-Zsanitizer=address` (which assumes
+  // unwind) and `-Clinker-plugin-lto` (the PE is final-linked here, not
+  // deferred to bun's lld link) don't apply, and `-Cforce-frame-pointers` /
+  // `-Ctarget-cpu` cost size we don't want. Replace with a freestanding
+  // flag set:
+  //   - `/ENTRY:shim_main`      — bypass the CRT (`mainCRTStartup`) entirely;
+  //                               the launcher reads argv from TEB→PEB itself.
+  //   - `/SUBSYSTEM:CONSOLE`    — link.exe can't infer subsystem without a
+  //                               recognised entry symbol.
+  //   - `/NODEFAULTLIB`         — don't pull msvcrt/vcruntime/ucrt; the only
+  //                               imports are kernel32 + ntdll (named via
+  //                               `#[link]` on the externs).
+  //
+  // (`-Cforce-unwind-tables=no` would drop `.pdata`, but the
+  // `*-windows-msvc` target spec sets `requires_uwtable: true` so rustc
+  // rejects it. The section is ~3 KiB; not worth a custom target JSON.)
+  const { CARGO_ENCODED_RUSTFLAGS: _, ...shimEnv } = env;
+  shimEnv.CARGO_ENCODED_RUSTFLAGS = [
+    // `panic = "immediate-abort"` is the new (nightly ≥ 2025-12) spelling of
+    // the old `-Zbuild-std-features=panic_immediate_abort`: every panic call
+    // (incl. core::fmt-carrying assert/unreachable/unwrap) compiles to a
+    // bare trap with no `Arguments` payload.
+    "-Zunstable-options",
+    "-Cpanic=immediate-abort",
+    "-Clink-arg=/ENTRY:shim_main",
+    "-Clink-arg=/SUBSYSTEM:CONSOLE",
+    "-Clink-arg=/NODEFAULTLIB",
+    "-Clink-arg=kernel32.lib",
+    "-Clink-arg=ntdll.lib",
+    // Cross-compiling from a unix host: this is the only cargo-driven link
+    // of a *target* artifact, and the linker is lld-link (no MSVC install),
+    // so point it at the xwin splat for the kernel32/ntdll import libs.
+    ...(cfg.winsysroot !== undefined ? [`-Clink-arg=/winsysroot:${cfg.winsysroot}`] : []),
+  ].join("\x1f");
+  // Declared output = per-build-dir stamp; the shared source-tree exe is an
+  // implicit INPUT (see the rust_shim rule comment for why). The exe must
+  // exist before ninja evaluates the graph — pre-create an empty
+  // placeholder the same way `src/install/build.rs` does for bare
+  // `cargo check`, so a fresh checkout doesn't error on a missing input.
+  if (!existsSync(shimDest)) {
+    mkdirSync(dirname(shimDest), { recursive: true });
+    writeFileSync(shimDest, "");
+  }
+  const shimStamp = resolve(targetDir, triple, "shim", "bun_shim_impl.stamp");
+  n.build({
+    outputs: [shimStamp],
+    rule: "rust_shim",
+    inputs: [],
+    // Same staleness signal as the main build (any .rs / Cargo.toml change
+    // re-invokes; cargo's own fingerprinting decides what actually
+    // recompiles). vendorStamps order the lol-html fetch first — the shim
+    // crate doesn't depend on lol-html, but cargo refuses to load the
+    // workspace manifest if any path-dep's `Cargo.toml` is missing.
+    // shimDest: rebuilt when a sibling build dir (other arch/profile)
+    // overwrote the shared exe.
+    implicitInputs: [cfg.cargo, ...inputs.rustSources, ...inputs.vendorStamps, shimDest],
+    vars: {
+      cwd: cfg.cwd,
+      args: quoteArgs(shimArgs, hostWin),
+      shim_src: quote(shimSrc, hostWin),
+      shim_dest: quote(shimDest, hostWin),
+      env: Object.entries(shimEnv)
+        .map(([k, v]) => `--env=${k}=${quote(v, hostWin)}`)
+        .join(" "),
+    },
+  });
+  n.phony("bun-shim", [shimStamp]);
+  return [shimStamp];
+}
+
 /**
  * Emit the cargo build step. Returns the output staticlib path as a
  * one-element array so the link step can spread it alongside the C++
@@ -780,118 +904,7 @@ export function emitRust(n: Ninja, cfg: Config, inputs: RustBuildInputs): string
   const tier3 = rustTargetIsTier3(rustTarget(cfg));
   const { args, env, targetDir, triple } = cargoBuildInvocation(cfg);
 
-  // ─── Windows .bin/ shim PE ───
-  // Builds `src/install/windows-shim/bun_shim_impl.rs` as a freestanding release PE and wires the artifact into `include_bytes!`. Without this step `include_bytes!` embeds the
-  // 0-byte placeholder and `bun install` writes empty `.exe`s into
-  // `node_modules/.bin/`.
-  //
-  // Ordered before the main cargo build via `implicitInputs` below so the
-  // real PE is on disk when `bun_install` compiles. Same env as the main
-  // build (toolchain forwarding, CARGO_HOME) but no codegen dep — the shim
-  // crate's graph is bun_core/bun_sys/bun_string only.
-  const shimInputs: string[] = [];
-  if (cfg.windows) {
-    const shimDest = windowsShimDestPath(cfg);
-    // Always `--profile shim` (workspace `[profile.shim]`: panic=abort,
-    // opt-level=z, lto, codegen-units=1, strip) regardless of bun's own
-    // profile — a debug bun should still write release shims (matches Zig's
-    // unconditional `.ReleaseFast`).
-    //
-    // `-Zbuild-std=core,compiler_builtins` rebuilds the sysroot for the
-    // freestanding `#![no_std]` crate so LTO can inline across `core`;
-    // `panic_immediate_abort` makes every `panic!`/`unreachable!`/`assert!`
-    // (incl. those buried in `core::fmt`, slice indexing, `Option::unwrap`)
-    // compile to a bare `ud2`/`brk` with no `core::fmt::Arguments` payload —
-    // that machinery is otherwise the bulk of `.text`. Nightly + `rust-src`
-    // are guaranteed by `rust-toolchain.toml`.
-    const shimArgs: string[] = [
-      "-p",
-      "bun_shim_impl",
-      "--bin",
-      "bun_shim_impl",
-      "--features",
-      "shim_standalone",
-      "--target-dir",
-      targetDir,
-      "--target",
-      triple,
-      "--profile",
-      "shim",
-      "--locked",
-      "-Zbuild-std=core,compiler_builtins",
-      "-Zbuild-std-features=compiler-builtins-mem",
-    ];
-    const shimSrc = resolve(targetDir, triple, "shim", "bun_shim_impl.exe");
-    // Same env minus the main build's CARGO_ENCODED_RUSTFLAGS — the shim has
-    // its own panic strategy (abort) so `-Zsanitizer=address` (which assumes
-    // unwind) and `-Clinker-plugin-lto` (the PE is final-linked here, not
-    // deferred to bun's lld link) don't apply, and `-Cforce-frame-pointers` /
-    // `-Ctarget-cpu` cost size we don't want. Replace with a freestanding
-    // flag set:
-    //   - `/ENTRY:shim_main`      — bypass the CRT (`mainCRTStartup`) entirely;
-    //                               the launcher reads argv from TEB→PEB itself.
-    //   - `/SUBSYSTEM:CONSOLE`    — link.exe can't infer subsystem without a
-    //                               recognised entry symbol.
-    //   - `/NODEFAULTLIB`         — don't pull msvcrt/vcruntime/ucrt; the only
-    //                               imports are kernel32 + ntdll (named via
-    //                               `#[link]` on the externs).
-    //
-    // (`-Cforce-unwind-tables=no` would drop `.pdata`, but the
-    // `*-windows-msvc` target spec sets `requires_uwtable: true` so rustc
-    // rejects it. The section is ~3 KiB; not worth a custom target JSON.)
-    const { CARGO_ENCODED_RUSTFLAGS: _, ...shimEnv } = env;
-    shimEnv.CARGO_ENCODED_RUSTFLAGS = [
-      // `panic = "immediate-abort"` is the new (nightly ≥ 2025-12) spelling of
-      // the old `-Zbuild-std-features=panic_immediate_abort`: every panic call
-      // (incl. core::fmt-carrying assert/unreachable/unwrap) compiles to a
-      // bare trap with no `Arguments` payload.
-      "-Zunstable-options",
-      "-Cpanic=immediate-abort",
-      "-Clink-arg=/ENTRY:shim_main",
-      "-Clink-arg=/SUBSYSTEM:CONSOLE",
-      "-Clink-arg=/NODEFAULTLIB",
-      "-Clink-arg=kernel32.lib",
-      "-Clink-arg=ntdll.lib",
-      // Cross-compiling from a unix host: this is the only cargo-driven link
-      // of a *target* artifact, and the linker is lld-link (no MSVC install),
-      // so point it at the xwin splat for the kernel32/ntdll import libs.
-      ...(cfg.winsysroot !== undefined ? [`-Clink-arg=/winsysroot:${cfg.winsysroot}`] : []),
-    ].join("\x1f");
-    // Declared output = per-build-dir stamp; the shared source-tree exe is an
-    // implicit INPUT (see the rust_shim rule comment for why). The exe must
-    // exist before ninja evaluates the graph — pre-create an empty
-    // placeholder the same way `src/install/build.rs` does for bare
-    // `cargo check`, so a fresh checkout doesn't error on a missing input.
-    if (!existsSync(shimDest)) {
-      mkdirSync(dirname(shimDest), { recursive: true });
-      writeFileSync(shimDest, "");
-    }
-    const shimStamp = resolve(targetDir, triple, "shim", "bun_shim_impl.stamp");
-    n.build({
-      outputs: [shimStamp],
-      rule: "rust_shim",
-      inputs: [],
-      // Same staleness signal as the main build (any .rs / Cargo.toml change
-      // re-invokes; cargo's own fingerprinting decides what actually
-      // recompiles). vendorStamps order the lol-html fetch first — the shim
-      // crate doesn't depend on lol-html, but cargo refuses to load the
-      // workspace manifest if any path-dep's `Cargo.toml` is missing.
-      // shimDest: rebuilt when a sibling build dir (other arch/profile)
-      // overwrote the shared exe.
-      implicitInputs: [cfg.cargo, ...inputs.rustSources, ...inputs.vendorStamps, shimDest],
-      vars: {
-        cwd: cfg.cwd,
-        args: quoteArgs(shimArgs, hostWin),
-        shim_src: quote(shimSrc, hostWin),
-        shim_dest: quote(shimDest, hostWin),
-        env: Object.entries(shimEnv)
-          .map(([k, v]) => `--env=${k}=${quote(v, hostWin)}`)
-          .join(" "),
-      },
-    });
-    n.phony("bun-shim", [shimStamp]);
-    shimInputs.push(shimStamp);
-  }
+  const shimInputs = emitWindowsShim(n, cfg, inputs);
 
   // ─── Emit build node ───
   // When the toolchain is rustup-managed and pinned, route through
diff --git a/src/bun_alloc/lib.rs b/src/bun_alloc/lib.rs
index fa01c50..cadafc2 100644
--- a/src/bun_alloc/lib.rs
+++ b/src/bun_alloc/lib.rs
@@ -2526,7 +2526,8 @@ impl<const COUNT: usize, const ITEM_LENGTH: usize> BSSStringList<COUNT, ITEM_LEN
 // ──────────────────────────────────────────────────────────────────────────
 
 pub struct BSSMapInner<ValueType, const COUNT: usize, const REMOVE_TRAILING_SLASHES: bool> {
-    pub(crate) index: IndexMap,
+    index: HashMap<HashKeyType, (IndexType, u64), IndexMapHasher>,
+    cache_epoch: u64,
     pub overflow_list: OverflowList<ValueType, BSS_OVERFLOW_BLOCK_SIZE>,
     pub(crate) mutex: Mutex,
     // Only `[0..backing_buf_used]` is initialized.
@@ -2552,7 +2553,8 @@ impl<ValueType, const COUNT: usize, const REMOVE_TRAILING_SLASHES: bool>
         // intentionally left uninitialized.
         unsafe {
             addr_of_mut!((*slot).mutex).write(Mutex::new());
-            addr_of_mut!((*slot).index).write(IndexMap::default());
+            addr_of_mut!((*slot).index).write(HashMap::default());
+            addr_of_mut!((*slot).cache_epoch).write(0);
             addr_of_mut!((*slot).backing_buf_used).write(0);
             OverflowList::init_counters_at(addr_of_mut!((*slot).overflow_list));
         }
@@ -2589,11 +2591,12 @@ impl<ValueType, const COUNT: usize, const REMOVE_TRAILING_SLASHES: bool>
         let _guard = self.mutex.lock();
         match self.index.entry(_key) {
             std::collections::hash_map::Entry::Occupied(e) => {
-                let v = *e.get();
+                let (v, epoch) = *e.get();
                 Ok(Result {
                     hash: _key,
                     index: v,
                     status: match v.index() {
+                        _ if epoch != self.cache_epoch => ItemStatus::Unknown,
                         i if i == NOT_FOUND.index() => ItemStatus::NotFound,
                         i if i == UNASSIGNED.index() => ItemStatus::Unknown,
                         _ => ItemStatus::Exists,
@@ -2601,7 +2604,7 @@ impl<ValueType, const COUNT: usize, const REMOVE_TRAILING_SLASHES: bool>
                 })
             }
             std::collections::hash_map::Entry::Vacant(e) => {
-                e.insert(UNASSIGNED);
+                e.insert((UNASSIGNED, self.cache_epoch));
                 Ok(Result {
                     hash: _key,
                     index: UNASSIGNED,
@@ -2618,13 +2621,17 @@ impl<ValueType, const COUNT: usize, const REMOVE_TRAILING_SLASHES: bool>
         // we dereference `index`. `MutexGuard` holds a raw pointer (see [`Mutex`] docs),
         // so it does not conflict with the `&mut self` borrow in `at_index`.
         let _guard = self.mutex.lock();
-        let index = self.index.get(&_key).copied()?;
+        let (index, epoch) = self.index.get(&_key).copied()?;
+        if epoch != self.cache_epoch {
+            return None;
+        }
         self.at_index(index)
     }
 
     pub fn mark_not_found(&mut self, result: Result) {
         let _guard = self.mutex.lock();
-        self.index.insert(result.hash, NOT_FOUND);
+        self.index
+            .insert(result.hash, (NOT_FOUND, self.cache_epoch));
     }
 
     pub fn at_index(&mut self, index: IndexType) -> Option<&mut ValueType> {
@@ -2677,10 +2684,37 @@ impl<ValueType, const COUNT: usize, const REMOVE_TRAILING_SLASHES: bool>
             // SAFETY: just initialized on the line above.
             unsafe { self.backing_buf[idx].assume_init_mut() }
         };
-        self.index.insert(result.hash, result.index);
+        self.index
+            .insert(result.hash, (result.index, self.cache_epoch));
         Ok(ret)
     }
 
+    /// Invalidate lookups while retaining each key's arena slot for reuse.
+    /// Negative entries are invalidated too. The next `get_or_put` reports
+    /// `Unknown` with the old index, so `put` replaces that slot instead of
+    /// appending another copy of every cache value on each host session.
+    ///
+    /// # Safety
+    /// All users of the previous values and outstanding `Result`s must have
+    /// finished. Re-population may overwrite the same stable slot in place.
+    pub unsafe fn invalidate_all(&mut self, mut invalidate: impl FnMut(&mut ValueType)) {
+        let _guard = self.mutex.lock();
+        for slot in &mut self.backing_buf[..usize::from(self.backing_buf_used)] {
+            // SAFETY: the initialized prefix is tracked by backing_buf_used.
+            invalidate(unsafe { slot.assume_init_mut() });
+        }
+        for position in 0..self.overflow_list.len() {
+            let mut index = IndexType::default();
+            index.set_is_overflow(true);
+            index.set_index(position);
+            invalidate(self.overflow_list.at_index_mut(index));
+        }
+        self.cache_epoch = self
+            .cache_epoch
+            .checked_add(1)
+            .expect("cache epoch exhausted");
+    }
+
     /// Returns true if the entry was removed.
     pub fn remove(&mut self, denormalized_key: &[u8]) -> bool {
         let _guard = self.mutex.lock();
diff --git a/src/bun_bin/Cargo.toml b/src/bun_bin/Cargo.toml
index 94839e8d..ab3ed68b 100644
--- a/src/bun_bin/Cargo.toml
+++ b/src/bun_bin/Cargo.toml
@@ -8,10 +8,22 @@ edition.workspace = true
 # set, exactly where `bun-zig.o` used to go. The C entry point (`main`) is
 # `#[no_mangle] extern "C"` in lib.rs; the clang++ driver provides crt1.o
 # (`_start` → `main`).
+#
+# `rlib` is the embedding form. A second Rust `staticlib` linked into the host
+# application would bundle its own `std`, panic runtime and allocator; the
+# static embedding product (`emitBun`'s "embed-native" mode) therefore consumes
+# this crate as an rlib inside the host's own crate graph, so rustc reconciles
+# one `std` and the single global allocator below across the whole image.
 [lib]
 name = "bun_rust"
 path = "lib.rs"
-crate-type = ["staticlib"]
+crate-type = ["staticlib", "rlib"]
+
+[features]
+# Embedding host product: drop the CLI entry point (`main`) and the argv /
+# process policy that only belongs to a `bun` executable. The embedded
+# initializer (`bun_embedded_initialize_process`) is always compiled.
+solid-gpui-embed = ["bun_runtime/solid-gpui-embed"]
 
 [lints]
 workspace = true
diff --git a/src/bun_bin/lib.rs b/src/bun_bin/lib.rs
index 20d6495..3330acf 100644
--- a/src/bun_bin/lib.rs
+++ b/src/bun_bin/lib.rs
@@ -33,6 +33,7 @@
 
 #![warn(unused_must_use)]
 
+#[cfg(not(feature = "solid-gpui-embed"))]
 use core::ffi::{c_char, c_int};
 
 mod c_abi_exports;
@@ -40,7 +41,12 @@ mod c_abi_exports;
 // Force-link `bun_platform` so its `#[no_mangle]` C exports
 // (`sys_epoll_pwait2`, …) reach the linker.
 use bun_platform as _;
+// CLI main normally creates this dependency edge. Embedding exports live in
+// bun_runtime, so retain the crate even when the CLI entrypoint is excluded;
+// staticlib must include its C ABI exports and transitive JSC bindings.
+use bun_runtime as _;
 
+#[cfg(not(feature = "solid-gpui-embed"))]
 use bun_core::Global;
 use bun_core::StackCheck;
 use bun_core::output;
@@ -149,6 +155,7 @@ pub(crate) extern "C" fn __lsan_default_suppressions() -> *const core::ffi::c_ch
 /// # Safety
 /// `argv` must point to `argc` valid NUL-terminated C strings that live for
 /// the entire process — guaranteed by the C runtime that calls this symbol.
+#[cfg(not(feature = "solid-gpui-embed"))]
 #[unsafe(no_mangle)]
 pub(crate) unsafe extern "C" fn main(argc: c_int, argv: *const *const c_char) -> c_int {
     // 0. Capture argv FIRST — before the crash handler, whose panic path
@@ -236,3 +243,21 @@ fn use_mimalloc_in_dependencies() {
         );
     }
 }
+
+
+/// Initialize process-wide Bun facilities once, on the persistent embedded owner
+/// thread. VM sessions call this again only to configure their thread stack.
+#[unsafe(no_mangle)]
+pub extern "C" fn bun_embedded_initialize_process() {
+    static INITIALIZE: std::sync::Once = std::sync::Once::new();
+    INITIALIZE.call_once(|| {
+        // The argv slice is empty and its storage is therefore never read.
+        unsafe { bun_core::init_argv(0, core::ptr::null()) };
+        bun_crash_handler::init();
+        use_mimalloc_in_dependencies();
+        #[cfg(windows)]
+        bun_core::handle_oom(bun_sys::windows::env::convert_env_to_wtf8());
+        output::stdio::init();
+    });
+    StackCheck::configure_thread();
+}
diff --git a/src/jsc/PosixSignalHandle.rs b/src/jsc/PosixSignalHandle.rs
index 436ca7b..0151926 100644
--- a/src/jsc/PosixSignalHandle.rs
+++ b/src/jsc/PosixSignalHandle.rs
@@ -62,10 +62,10 @@ impl PosixSignalHandle {
         // Publish the new tail (Release so that the consumer sees the updated tail).
         self.tail.store(old_tail.wrapping_add(1), Ordering::Release);
 
-        if let Some(vm) = VirtualMachine::get_main_thread_vm() {
-            // SAFETY: `event_loop()` returns the VM-owned EventLoop; live for VM lifetime.
+        VirtualMachine::with_main_thread_vm(|vm| {
+            // SAFETY: the signal-safe weak gate keeps VM and loop alive.
             unsafe { (*(*vm).event_loop()).wakeup() };
-        }
+        });
 
         true
     }
@@ -125,19 +125,14 @@ extern "C" fn Bun__onPosixSignal(number: i32) {
             // SAFETY: `_exit(2)` is async-signal-safe and takes no pointers.
             unsafe { libc::_exit(0) };
         }
-        let Some(vm) = VirtualMachine::get_main_thread_vm() else {
-            return;
-        };
-        // SAFETY: `vm` and its event loop are process-lifetime; raw place
-        // projection reads only the `signal_handler` slot (no `&EventLoop`
-        // formed — the main thread may hold `&mut EventLoop` concurrently).
-        let handler = unsafe { (*(*vm).event_loop()).signal_handler };
-        if let Some(handler) = handler {
-            // `BackRef::deref` is the centralised set-once-NonNull proof; the
-            // pointee is all-atomic (`Sync`), so a `&PosixSignalHandle` from
-            // async-signal context is sound.
-            let _ = handler.enqueue(u8::try_from(number).expect("int cast"));
-        }
+        VirtualMachine::with_main_thread_vm(|vm| {
+            // SAFETY: the signal-safe weak gate keeps VM and handler alive;
+            // raw field reads do not borrow the concurrently running loop.
+            let handler = unsafe { (*(*vm).event_loop()).signal_handler };
+            if let Some(handler) = handler {
+                let _ = handler.enqueue(u8::try_from(number).expect("int cast"));
+            }
+        });
     }
     #[cfg(not(unix))]
     let _ = number;
@@ -271,14 +266,14 @@ impl PosixSignalTask {
 extern "C" fn Bun__ensureSignalHandler() {
     #[cfg(unix)]
     {
-        if let Some(vm) = VirtualMachine::get_main_thread_vm() {
-            // SAFETY: `vm` and its event loop are process-lifetime.
+        VirtualMachine::with_main_thread_vm(|vm| {
+            // SAFETY: called on the VM thread, within the weak lifetime gate.
             let this = unsafe { &mut *(*vm).event_loop() };
             if this.signal_handler.is_none() {
                 let boxed = PosixSignalHandle::new(PosixSignalHandle::default());
                 this.signal_handler =
                     Some(bun_ptr::BackRef::from(bun_core::heap::into_raw_nn(boxed)));
             }
-        }
+        });
     }
 }
diff --git a/src/jsc/VirtualMachine.rs b/src/jsc/VirtualMachine.rs
index 3a0e43b..1691468 100644
--- a/src/jsc/VirtualMachine.rs
+++ b/src/jsc/VirtualMachine.rs
@@ -225,6 +225,8 @@ pub struct VirtualMachine {
     pub(crate) has_run_cleanup_hooks: bool,
     pub plugin_runner: Option<crate::plugin_runner::PluginRunner>,
     pub is_main_thread: bool,
+    /// The host owns this VM lifetime; process.exit stops this VM only.
+    pub is_embedded: bool,
     pub exit_handler: ExitHandler,
 
     pub(crate) default_tls_reject_unauthorized: Option<bool>,
@@ -451,6 +453,10 @@ pub(crate) struct VMHolder;
 
 static MAIN_THREAD_VM: core::sync::atomic::AtomicPtr<VirtualMachine> =
     core::sync::atomic::AtomicPtr::new(core::ptr::null_mut());
+// Signal-safe weak accesses: retirement unpublishes the pointer, then waits for
+// callers that entered before it. SeqCst pairs both store/load orderings.
+static MAIN_THREAD_VM_ACCESS: core::sync::atomic::AtomicUsize =
+    core::sync::atomic::AtomicUsize::new(0);
 
 // `#[thread_local]` (bare `__thread` slot) instead of `thread_local!` macro:
 // `LocalKey::__getit` adds a lazy-init check + on some targets a
@@ -798,9 +804,17 @@ impl VirtualMachine {
         VM.get()
     }
 
-    pub(crate) fn get_main_thread_vm() -> Option<*mut VirtualMachine> {
-        let p = MAIN_THREAD_VM.load(core::sync::atomic::Ordering::Acquire);
-        if p.is_null() { None } else { Some(p) }
+    pub(crate) fn with_main_thread_vm<R>(f: impl FnOnce(*mut VirtualMachine) -> R) -> Option<R> {
+        struct Access;
+        impl Drop for Access {
+            fn drop(&mut self) {
+                MAIN_THREAD_VM_ACCESS.fetch_sub(1, core::sync::atomic::Ordering::SeqCst);
+            }
+        }
+        MAIN_THREAD_VM_ACCESS.fetch_add(1, core::sync::atomic::Ordering::SeqCst);
+        let _access = Access;
+        let p = MAIN_THREAD_VM.load(core::sync::atomic::Ordering::SeqCst);
+        if p.is_null() { None } else { Some(f(p)) }
     }
 
     #[inline]
@@ -1537,6 +1551,12 @@ impl VirtualMachine {
 
         let hooks = runtime_hooks().expect("RuntimeHooks not installed");
         if self.is_handling_uncaught_exception {
+            if self.is_embedded {
+                self.run_error_handler(err, None);
+                self.exit_handler.exit_code = 7;
+                self.handle_ref().request_termination();
+                return false;
+            }
             if !self.is_main_thread() {
                 // node parity: a throw inside the uncaughtException handler in a
                 // worker exits the worker with code 1 (not the main-thread fatal
@@ -1573,12 +1593,18 @@ impl VirtualMachine {
                 self.is_handling_uncaught_exception = false;
                 // SAFETY: see above.
                 unsafe { (hooks.process_exit)(global_object.as_ptr(), 1) };
+                if self.is_embedded {
+                    return false;
+                }
                 panic!("made it past process.exit()");
             }
             // TODO maybe we want a separate code path for uncaught exceptions
             self.unhandled_error_counter += 1;
             self.exit_handler.exit_code = 1;
             (self.on_unhandled_rejection)(self, global_object, err);
+            if self.is_embedded {
+                self.handle_ref().request_termination();
+            }
         }
         // Note: this reset must cover BOTH the FFI call and the
         // `onUnhandledRejection` callback above. The flag must stay raised
@@ -1745,6 +1771,46 @@ impl VirtualMachine {
         bun_core::Global::exit(u32::from(self.exit_handler.exit_code))
     }
 
+    /// Consume an embedded VM on its owner thread, outside every JS frame.
+    /// Exit handlers have already run and all host-owned Strong handles are gone.
+    /// The caller must never dereference `this` after this function returns.
+    pub unsafe fn teardown_embedded(this: *mut Self) -> u8 {
+        // No signal handler may retain the retiring VM while it is destroyed.
+        let _ = MAIN_THREAD_VM.compare_exchange(
+            this, core::ptr::null_mut(),
+            core::sync::atomic::Ordering::SeqCst,
+            core::sync::atomic::Ordering::SeqCst,
+        );
+        while MAIN_THREAD_VM_ACCESS.load(core::sync::atomic::Ordering::SeqCst) != 0 {
+            std::thread::yield_now();
+        }
+        // SAFETY: caller owns the live VM on this thread. Signal accesses have
+        // left; every remaining cross-thread access is gated by VmHandle.
+        let code = unsafe { (*this).exit_handler.exit_code };
+        unsafe { Self::teardown(this, Teardown::Embedded) };
+        // `destroy` dropped fields; the raw allocation is deliberately not a
+        // Box<VirtualMachine>, because dropping its fields again would be UB.
+        unsafe {
+            let console = core::mem::replace(&mut (*this).console, core::ptr::null_mut());
+            if !console.is_null() {
+                bun_core::heap::destroy(console);
+            }
+            if let Some(log) = (*this).log.take() {
+                bun_core::heap::destroy(log.as_ptr());
+            }
+            #[cfg(unix)]
+            if let Some(signal) = (*this).regular_event_loop.signal_handler.take() {
+                let pointer: core::ptr::NonNull<crate::posix_signal_handle::PosixSignalHandle> = signal.into();
+                drop(bun_core::heap::take(pointer.as_ptr()));
+            }
+            VMHolder::set_vm(None);
+            VMHolder::set_cached_global_object(None);
+            IS_MAIN_THREAD_VM.set(false);
+            std::alloc::dealloc(this.cast(), core::alloc::Layout::new::<Self>());
+        }
+        code
+    }
+
     /// Checkpoint + close the sqlite connections *this* VM opened, while it is
     /// alive and no user script will touch them again. Never another VM's: a
     /// worker still running when the main thread exits without joining it owns
@@ -1817,7 +1883,7 @@ impl VirtualMachine {
         // The exiting main thread neither closes its loop nor may nest uv_run
         // here: process.exit() can be running inside a libuv completion callback.
         #[cfg(windows)]
-        if matches!(kind, Teardown::Worker) {
+        if matches!(kind, Teardown::Worker | Teardown::Embedded) {
             while bun_sys::windows::libuv::Loop::drain_requests() {
                 sweep();
             }
@@ -1873,6 +1939,16 @@ impl VirtualMachine {
             (*this).release_queued_work();
             let _ = Self::stop_phase_sweep(this, kind);
         });
+        // These VM-owned collections contain JSC Strong handles. Once every
+        // off-thread completion has returned they can be released, while the
+        // heap still exists, before the final collection.
+        unsafe {
+            drop(core::mem::take(&mut (*this).commonjs_custom_extensions));
+            drop(core::mem::take(&mut (*this).modules));
+            drop(core::mem::take(&mut (*this).macros));
+            (*this).main_resolved_path.deref();
+            (*this).main_resolved_path = bun_core::String::empty();
+        }
         // The exiting main thread now parks the process-wide HTTP thread —
         // after the children it also served are joined and this VM's own
         // requests are back — so it cannot touch what process exit frees. If
@@ -1891,7 +1967,7 @@ impl VirtualMachine {
 
         // ---- C. JSC VM -------------------------------------------------------
         match kind {
-            Teardown::Worker => WebWorker__teardownJSCVM(vm.global()),
+            Teardown::Worker | Teardown::Embedded => WebWorker__teardownJSCVM(vm.global()),
             Teardown::MainThreadExit => Zig__GlobalObject__destructOnExit(vm.global()),
         }
         // Finalizers just closed the sockets only they owned; `us_socket_close`
@@ -1906,7 +1982,7 @@ impl VirtualMachine {
         teardown_log!("teardown: JSC VM destroyed");
 
         // ---- D. loops (worker; main exits the process instead) ----------------
-        if matches!(kind, Teardown::Worker) {
+        if matches!(kind, Teardown::Worker | Teardown::Embedded) {
             // Whatever B/C unref'd through the concurrent counter, folded now:
             // on Windows it shares `active_handles` with libuv and a residue
             // would keep the loop close below spinning.
@@ -1957,7 +2033,7 @@ impl VirtualMachine {
         // main thread keeps its loop (the OS reclaims the handles); sweeping
         // them there only re-enters stream owners under still-running script.
         #[cfg(windows)]
-        if matches!(kind, Teardown::Worker) {
+        if matches!(kind, Teardown::Worker | Teardown::Embedded) {
             bun_sys::windows::libuv::open_handles::stop_all_for_vm_teardown();
         }
         let _ = kind;
@@ -1991,6 +2067,8 @@ impl VirtualMachine {
 #[derive(Clone, Copy, PartialEq, Eq)]
 pub(crate) enum Teardown {
     Worker,
+    /// Full VM/loop cleanup while the process and owner thread remain alive.
+    Embedded,
     MainThreadExit,
 }
 
@@ -2346,6 +2424,7 @@ unsafe extern "C" {
     // safe: `console`/`worker_ptr` are opaque round-trip pointers C++ stores
     // into the new ZigGlobalObject (never dereferenced as Rust data); remaining
     // args are by-value scalars.
+    safe fn Bun__reserveMainContextIdentifier() -> u32;
     safe fn Zig__GlobalObject__create(
         console: *mut c_void,
         context_id: i32,
@@ -2426,9 +2505,9 @@ impl VirtualMachine {
             p.cast()
         };
         VM.set(Some(vm));
-        if opts.is_main_thread {
-            MAIN_THREAD_VM.store(vm, core::sync::atomic::Ordering::Release);
-        }
+        // Publish to process signal handlers only once every field and loop
+        // is initialized, so a signal from a previous session cannot observe
+        // partially initialized storage.
 
         // ConsoleObject is self-referential (buffers + adapters) — allocate
         // stable storage and init in place.
@@ -2445,9 +2524,16 @@ impl VirtualMachine {
         let console =
             bun_core::heap::into_raw(console_box).cast::<crate::console_object::ConsoleObject>();
 
-        let context_id = opts
-            .context_id
-            .unwrap_or(if opts.is_main_thread { 1 } else { i32::MAX });
+        let context_id = opts.context_id.unwrap_or_else(|| {
+            if opts.is_main_thread {
+                // Shared allocator with Workers: only the first main VM uses
+                // the unique initial-context constructor. Later sessions get
+                // new routing identities while retaining their main role.
+                Bun__reserveMainContextIdentifier() as i32
+            } else {
+                i32::MAX
+            }
+        });
 
         // SAFETY: `vm` is a fresh unique zeroed allocation on this thread. All
         // writes go through `addr_of_mut!` so no `&mut VirtualMachine` is
@@ -2553,7 +2639,40 @@ impl VirtualMachine {
             // so abort `init` here — `vm.transpiler` was never written, and
             // bailing out before the CLI reads it turns the old segfault into a
             // clean error + non-zero exit.
-            unsafe { (*vm).runtime_state = (hooks.init_runtime_state)(vm, &mut opts)? };
+            match unsafe { (hooks.init_runtime_state)(vm, &mut opts) } {
+                Ok(state) => unsafe { (*vm).runtime_state = state },
+                Err(error) => {
+                    // The hook unwound its runtime state; no JSC heap exists.
+                    // Only initialized fields may drop (transpiler is still
+                    // uninitialized). No handle was published to another thread.
+                    unsafe {
+                        (*vm).handle.close_and_wait(|| {});
+                        core::mem::ManuallyDrop::drop(&mut (*vm).handle);
+                        macro_rules! drop_initialized {
+                            ($($field:ident),* $(,)?) => {$(
+                                core::ptr::drop_in_place(core::ptr::addr_of_mut!((*vm).$field));
+                            )*};
+                        }
+                        drop_initialized!(
+                            preload, child_workers, argv, resolved_path_dups,
+                            macros, macro_entry_points, auto_killer,
+                            commonjs_custom_extensions, entry_point, origin,
+                            ref_strings, modules, macro_event_loop, proxy_env_storage,
+                            gc_controller, channel_ref, remap_stack_frames_mutex,
+                            ref_strings_mutex, transpiler_store, regular_event_loop,
+                            source_mappings,
+                        );
+                        bun_core::heap::destroy(console);
+                        if opts.log.is_none() {
+                            bun_core::heap::destroy(log);
+                        }
+                        VMHolder::set_vm(None);
+                        VMHolder::set_cached_global_object(None);
+                        std::alloc::dealloc(vm.cast(), layout);
+                    }
+                    return Err(error);
+                }
+            }
         }
 
         // JSGlobalObject creation. `ensure_waker()` must run before the FFI.
@@ -2612,6 +2731,9 @@ impl VirtualMachine {
             IS_SMOL_MODE.store(true, core::sync::atomic::Ordering::Relaxed);
         }
 
+        if opts.is_main_thread {
+            MAIN_THREAD_VM.store(vm, core::sync::atomic::Ordering::SeqCst);
+        }
         Ok(vm)
     }
 
@@ -4791,6 +4913,12 @@ impl VirtualMachine {
             // once on the same thread; `self` is the live per-thread VM.
             unsafe { (hooks.deinit_runtime_state)(std::ptr::from_mut(self), state) };
         }
+        // The owner raw-deallocates the VM. Reclaim the remaining owned
+        // container storage explicitly; their elements no longer own JS roots.
+        drop(core::mem::take(&mut self.argv));
+        drop(core::mem::take(&mut self.child_workers));
+        drop(core::mem::take(&mut self.ref_strings));
+        drop(core::mem::take(&mut self.macro_entry_points));
         self.has_terminated = true;
     }
     /// Note: takes the concrete
diff --git a/src/jsc/bindings/ScriptExecutionContext.cpp b/src/jsc/bindings/ScriptExecutionContext.cpp
index a6fa3fc..5bc03c3 100644
--- a/src/jsc/bindings/ScriptExecutionContext.cpp
+++ b/src/jsc/bindings/ScriptExecutionContext.cpp
@@ -19,6 +19,11 @@ namespace WebCore {
 static constexpr ScriptExecutionContextIdentifier INITIAL_IDENTIFIER_INTERNAL = 1;
 
 static std::atomic<unsigned> lastUniqueIdentifier = INITIAL_IDENTIFIER_INTERNAL;
+// Main is a role, not an ID reused by every embedded VM. Retired session IDs
+// remain retired so delayed Worker/timer/context tasks cannot reach a new VM.
+static std::atomic<ScriptExecutionContextIdentifier> mainContextIdentifier { INITIAL_IDENTIFIER_INTERNAL };
+static bool hasReservedMainContextIdentifier = false; // contexts map lock
+
 
 #if ASSERT_ENABLED
 static ScriptExecutionContextIdentifier initialIdentifier()
@@ -71,6 +76,25 @@ static HashMap<ScriptExecutionContextIdentifier, ScriptExecutionContext*>& allSc
     return contexts;
 }
 
+// Called by VM initialization before JSC creates GC timers or its global, so
+// Rust's timer metadata and the C++ context agree on the same reserved ID.
+extern "C" uint32_t Bun__reserveMainContextIdentifier()
+{
+    Locker locker { allScriptExecutionContextsMapLock };
+    RELEASE_ASSERT_WITH_MESSAGE(!allScriptExecutionContextsMap().contains(mainContextIdentifier.load()), "a main VM context is already active");
+    auto identifier = hasReservedMainContextIdentifier
+        ? ScriptExecutionContext::generateIdentifier()
+        : INITIAL_IDENTIFIER_INTERNAL;
+    hasReservedMainContextIdentifier = true;
+    mainContextIdentifier.store(identifier, std::memory_order_release);
+    return identifier;
+}
+
+bool ScriptExecutionContext::isMainThread() const
+{
+    return m_identifier == mainContextIdentifier.load(std::memory_order_acquire);
+}
+
 ScriptExecutionContext* ScriptExecutionContext::getScriptExecutionContext(ScriptExecutionContextIdentifier identifier)
 {
     if (identifier == 0) {
@@ -291,7 +315,7 @@ bool ScriptExecutionContext::ensureOnMainThread(Function<void(ScriptExecutionCon
 ScriptExecutionContext* ScriptExecutionContext::getMainThreadScriptExecutionContext()
 {
     Locker locker { allScriptExecutionContextsMapLock };
-    return allScriptExecutionContextsMap().get(1);
+    return allScriptExecutionContextsMap().get(mainContextIdentifier.load(std::memory_order_acquire));
 }
 
 void ScriptExecutionContext::checkConsistency() const
diff --git a/src/jsc/bindings/ScriptExecutionContext.h b/src/jsc/bindings/ScriptExecutionContext.h
index 25e3e4f..1e9e54f 100644
--- a/src/jsc/bindings/ScriptExecutionContext.h
+++ b/src/jsc/bindings/ScriptExecutionContext.h
@@ -71,7 +71,7 @@ public:
     {
         return m_url;
     }
-    bool isMainThread() const { return m_identifier == 1; }
+    bool isMainThread() const;
     bool isContextThread();
 
     // Active objects are not garbage collected even if inaccessible, e.g. because their activity may result in callbacks being invoked.
diff --git a/src/jsc/bindings/c-bindings.cpp b/src/jsc/bindings/c-bindings.cpp
index 1ead598..c828a6f 100644
--- a/src/jsc/bindings/c-bindings.cpp
+++ b/src/jsc/bindings/c-bindings.cpp
@@ -1092,7 +1092,14 @@ struct BlobHeader {
 
 #if OS(DARWIN)
 
-extern "C" BlobHeader __attribute__((section("__BUN,__bun"))) BUN_COMPILED = { 0, 0 };
+// `used`/`retain`/`weak`: a native embedding host supplies this section with a
+// strong definition of its own (Rust `#[no_mangle]` in `__BUN,__bun`), and its
+// link may not reference the reader from a live path. `retain` keeps the section
+// under `-dead_strip`; `weak` makes this 8-byte placeholder the fallback that a
+// strong definition overrides, so `Bun__getStandaloneModuleGraphMachoLength()`
+// addresses the real payload while a bun-only build with no such definition
+// keeps working unchanged.
+extern "C" BlobHeader __attribute__((section("__BUN,__bun"), used, retain, weak)) BUN_COMPILED = { 0, 0 };
 
 extern "C" uint64_t* Bun__getStandaloneModuleGraphMachoLength()
 {
@@ -1132,10 +1139,15 @@ static bool initializePESection()
     PIMAGE_NT_HEADERS ntHeaders = (PIMAGE_NT_HEADERS)((BYTE*)hModule + dosHeader->e_lfanew);
     if (ntHeaders->Signature != IMAGE_NT_SIGNATURE) return false;
 
+    // Name is an 8-byte NUL-padded field, and sections are scanned in table
+    // order. A `.bun` prefix match also hits bun's own `.bunblt` builtin-module
+    // blob, so an image where that section precedes ours reads the wrong bytes
+    // as the payload length. Require the exact name.
+    static constexpr char BUN_SECTION_NAME[IMAGE_SIZEOF_SHORT_NAME] = { '.', 'b', 'u', 'n' };
     PIMAGE_SECTION_HEADER sectionHeader = IMAGE_FIRST_SECTION(ntHeaders);
 
     for (int i = 0; i < ntHeaders->FileHeader.NumberOfSections; i++) {
-        if (strncmp((char*)sectionHeader->Name, ".bun", 4) == 0) {
+        if (memcmp(sectionHeader->Name, BUN_SECTION_NAME, IMAGE_SIZEOF_SHORT_NAME) == 0) {
             // Found the .bun section
             // Section format: 8 bytes size (uint64_t) + data
             BYTE* sectionData = (BYTE*)hModule + sectionHeader->VirtualAddress;
diff --git a/src/jsc/event_loop.rs b/src/jsc/event_loop.rs
index be2b578..40c742f 100644
--- a/src/jsc/event_loop.rs
+++ b/src/jsc/event_loop.rs
@@ -844,6 +844,9 @@ impl EventLoop {
             "pending immediates must be released (release_queued_tasks) while the loop is alive"
         );
         self.tasks = Queue::init();
+        self.immediate_tasks = Vec::new();
+        self.next_immediate_tasks = Vec::new();
+        self.yield_tasks = Vec::new();
         // Free the deferred-task map's storage. The tasks must not be run, and an
         // entry owns nothing but a `Copy` ctx pointer whose owner released it when
         // the JSC teardown before this finalized it. A worker's VM box is
@@ -1041,23 +1044,17 @@ impl EventLoop {
     }
 
     pub fn wakeup(&self) {
+        // Off-thread callers hold VmHandle's weak gate. Read only the fixed
+        // loop pointer and call the concurrent-safe C wake; never mint an
+        // aliased &mut Loop while the owner is parked inside its poll.
         #[cfg(windows)]
-        {
-            if let Some(loop_) = self.uws_loop {
-                // SAFETY: uws_loop is a valid live uws::Loop handle
-                unsafe { (*loop_.as_ptr()).wakeup() };
-            }
-            return;
-        }
+        let pointer = self.uws_loop.map(NonNull::as_ptr);
         #[cfg(not(windows))]
-        {
-            // Route through the single audited `platform_loop_opt()` accessor
-            // (set-once `Option<*mut>` deref) instead of open-coding the raw
-            // `(*event_loop_handle).wakeup()` here. Same `&mut Loop` is formed
-            // either way (autoref), so no soundness change vs the prior code.
-            if let Some(loop_) = self.vm_ref().platform_loop_opt() {
-                loop_.wakeup();
-            }
+        let pointer = self.virtual_machine.and_then(|vm| unsafe {
+            (*vm.as_ptr()).event_loop_handle
+        });
+        if let Some(pointer) = pointer {
+            unsafe { bun_uws::us_wakeup_loop(pointer) };
         }
     }
 
diff --git a/src/resolver/lib.rs b/src/resolver/lib.rs
index 9e5c9c0..df31f69 100644
--- a/src/resolver/lib.rs
+++ b/src/resolver/lib.rs
@@ -1236,9 +1236,14 @@ pub mod fs {
                         // scrutinee directly so no second `&mut *cached_ptr` is materialized
                         // while the first is on the borrow stack (Stacked Borrows hygiene).
                         match unsafe { &mut *cached_ptr } {
-                            EntriesOption::Entries(e) if e.generation < generation => {
+                            EntriesOption::Entries(e)
+                                if cr.status == bun_alloc::ItemStatus::Unknown
+                                    || e.generation < generation =>
+                            {
                                 in_place = Some(std::ptr::from_mut::<DirEntry>(*e));
                             }
+                            EntriesOption::Err(_)
+                                if cr.status == bun_alloc::ItemStatus::Unknown => {}
                             cached => return Ok(cached),
                         }
                     } else if cr.status == bun_alloc::ItemStatus::NotFound && generation == 0 {
@@ -1342,6 +1347,18 @@ pub mod fs {
             })))
         }
 
+        /// Invalidate listings at a quiescent embedded-session boundary.
+        /// Re-reading a stale listing reuses its DirEntry and same-name Entry
+        /// slots; retained indexes are storage, not valid filesystem results.
+        ///
+        /// # Safety
+        /// No previous VM, resolver, or resolver worker may still use this cache.
+        pub(crate) unsafe fn invalidate_for_embedded_session(&mut self) {
+            let _guard = self.entries_mutex.lock_guard();
+            // SAFETY: caller guarantees all previous cache readers are gone.
+            unsafe { self.entries.inner().invalidate_all(|_| {}) };
+        }
+
         /// Evicts `file_path` from the directory-entry cache; returns whether
         /// an entry was removed.
         pub(crate) fn bust_entries_cache(&mut self, file_path: &[u8]) -> bool {
diff --git a/src/resolver/resolver.rs b/src/resolver/resolver.rs
index 1e45ac9..2ef2466 100644
--- a/src/resolver/resolver.rs
+++ b/src/resolver/resolver.rs
@@ -279,29 +279,18 @@ use bun_alloc as allocators;
 // (re-exported from `bun_ast`; see `result.rs`).
 use bun_ast::SideEffects;
 
-// ── Process-lifetime arenas for DirInfo-cached parses ─────────────────────
-// The DirInfo cache (`DirInfo::hash_map_instance()`) is a true process-lifetime
-// singleton; entries hold `&'static PackageJSON` / `&'static TSConfigJSON` and
-// borrow `&'static [u8]` source bytes. PORTING.md §Forbidden bars `Box::leak`/
-// `mem::forget` for this — process-lifetime storage must go through
-// `LazyLock`. These append-only arenas are that storage; the `Box<T>` heap
-// address is stable across `Vec` growth, so handing out `&'static T` is sound.
-
-/// Intern a parsed `PackageJSON` into the process-lifetime DirInfo arena.
-/// Returns `NonNull` (not `&'static`) so mut-provenance survives to write
-/// sites — handing out `&T` here and casting back to `*mut T` would be UB
-/// under Stacked Borrows.
+// ── Ownership for DirInfo-cached parses ──────────────────────────────────
+// Parsed package objects have stable heap addresses while resolver workers
+// share their DirInfo references. A normal CLI run keeps them until exit;
+// an embedding host reclaims them after all users of the old session stop.
+#[expect(clippy::vec_box)]
+static PACKAGE_JSON_ARENA: std::sync::LazyLock<bun_threading::Guarded<Vec<Box<PackageJSON>>>> =
+    std::sync::LazyLock::new(Default::default);
+
+/// Intern a parsed package without losing mutable pointer provenance.
 fn intern_package_json(pkg: PackageJSON) -> core::ptr::NonNull<PackageJSON> {
-    // `Box` is load-bearing: returns `NonNull<PackageJSON>` derived from the
-    // box interior, treated as `'static`; unboxing would dangle on `Vec` realloc.
-    #[expect(clippy::vec_box)]
-    static ARENA: std::sync::LazyLock<bun_threading::Guarded<Vec<Box<PackageJSON>>>> =
-        std::sync::LazyLock::new(Default::default);
-    let mut guard = ARENA.lock();
+    let mut guard = PACKAGE_JSON_ARENA.lock();
     guard.push(Box::new(pkg));
-    // SAFETY: ARENA is `'static` (LazyLock); entries are never removed; the
-    // `Box<PackageJSON>` heap address is stable across `Vec` reallocation.
-    // Derive from `&mut **last` so the returned pointer carries mut-provenance.
     core::ptr::NonNull::from(&mut **guard.last_mut().unwrap())
 }
 
@@ -902,6 +891,44 @@ impl<'a> Resolver<'a> {
         self.opts.global_cache.is_enabled()
     }
 
+    /// Start resolution for a new embedded VM after the old session stopped.
+    /// Both metadata/negative lookups and filesystem listings belong to a
+    /// session. Reuse their arena slots, but never reuse their old validity.
+    ///
+    /// # Safety
+    /// The host must have destroyed the previous VM and joined all of its
+    /// resolver workers before calling this, and must not start a new resolver
+    /// until it returns. Old DirInfo/Entry references may no longer be used.
+    pub unsafe fn begin_embedded_session() {
+        let _guard = RESOLVER_MUTEX.lock_guard();
+        // DirInfo owns its local tsconfig allocation; inherited config and
+        // package fields are borrows. Remove every old borrow before freeing
+        // the parse allocations, including unreachable old cache slots.
+        let mut tsconfigs = Vec::new();
+        // SAFETY: the embedding host guarantees quiescence between sessions.
+        unsafe {
+            (*DirInfo::hash_map_instance()).invalidate_all(|info| {
+                if let Some(config) = info.tsconfig_json.take() {
+                    // This is the final merged Box from dir_info_uncached.
+                    tsconfigs.push(bun_core::heap::take(config.as_ptr()));
+                }
+                *info = DirInfo::DirInfo::default();
+            });
+        }
+        for config in tsconfigs {
+            TSConfigJSON::destroy(config);
+        }
+        PACKAGE_JSON_ARENA.lock().clear();
+        if Fs::INSTANCE_LOADED.load(core::sync::atomic::Ordering::Acquire) {
+            // SAFETY: same quiescent session boundary, with resolver lock held.
+            unsafe {
+                Fs::FileSystem::instance()
+                    .fs
+                    .invalidate_for_embedded_session()
+            };
+        }
+    }
+
     pub fn init1(
         log: NonNull<bun_ast::Log>,
         _fs: *mut Fs::FileSystem,
@@ -3374,7 +3401,9 @@ impl<'a> Resolver<'a> {
 
         if let Some(cached_entry) = rfs!().entries.at_index(cached_dir_entry_result.index) {
             if let Fs::file_system::real_fs::EntriesOption::Entries(entries) = cached_entry {
-                if entries.generation >= self.generation {
+                if cached_dir_entry_result.status != allocators::ItemStatus::Unknown
+                    && entries.generation >= self.generation
+                {
                     dir_entries_option = cached_entry;
                     needs_iter = false;
                 } else {
@@ -4148,9 +4177,7 @@ impl<'a> Resolver<'a> {
         };
         let Some(pkg) = pkg else { return Ok(None) };
 
-        // NOTE: the DirInfo cache holds `&'static` refs. PORTING.md
-        // §Forbidden bars `Box::leak`; intern into the process-lifetime arena
-        // owned alongside the DirInfo singleton instead.
+        // DirInfo borrows the parse until the next quiescent session boundary.
         Ok(Some(intern_package_json(pkg)))
     }
 
@@ -4344,14 +4371,22 @@ impl<'a> Resolver<'a> {
                 fd: FD::INVALID,
             });
 
-            if let Some(top_entry) = rfs!().entries.get(top) {
+            // A stale listing still owns its immutable interned directory
+            // name. Reuse that storage across sessions, but not its old fd or
+            // error result, which must be checked against the filesystem.
+            let stored_entry = rfs!().entries.get_or_put(top)?;
+            if let Some(top_entry) = rfs!().entries.at_index(stored_entry.index) {
                 match top_entry {
                     Fs::file_system::real_fs::EntriesOption::Entries(entries) => {
                         // SAFETY: slot was written immediately above.
                         let slot = unsafe { queue[i].assume_init_mut() };
                         slot.safe_path = bun_ptr::RawSlice::new(entries.dir);
-                        slot.fd = entries.fd;
+                        if stored_entry.status != allocators::ItemStatus::Unknown {
+                            slot.fd = entries.fd;
+                        }
                     }
+                    Fs::file_system::real_fs::EntriesOption::Err(_)
+                        if stored_entry.status == allocators::ItemStatus::Unknown => {}
                     Fs::file_system::real_fs::EntriesOption::Err(err) => {
                         debuglog!(
                             "Failed to load DirEntry {}  {} - {}",
@@ -4633,7 +4668,9 @@ impl<'a> Resolver<'a> {
 
             if let Some(cached_entry) = rfs!().entries.at_index(cached_dir_entry_result.index) {
                 if let Fs::file_system::real_fs::EntriesOption::Entries(entries) = cached_entry {
-                    if entries.generation >= self.generation {
+                    if cached_dir_entry_result.status != allocators::ItemStatus::Unknown
+                        && entries.generation >= self.generation
+                    {
                         dir_entries_option = cached_entry;
                         needs_iter = false;
                     } else {
@@ -6627,7 +6664,9 @@ impl<'a> Resolver<'a> {
                         // SAFETY: parent_config_ptr came from TSConfigJSON::new (heap::alloc)
                         TSConfigJSON::destroy(unsafe { bun_core::heap::take(parent_config_ptr) });
                     }
-                    // `merged_config` is a leaked Box (heap::alloc) interned into DirInfo; outlives the resolver.
+                    // DirInfo owns this final merged Box. It outlives all
+                    // resolver workers and is reclaimed at the next quiescent
+                    // embedded-session boundary (a CLI keeps it until exit).
                     info.tsconfig_json = Some(
                         core::ptr::NonNull::new(merged_config).expect("heap::alloc is non-null"),
                     );
diff --git a/src/runtime/cli/mod.rs b/src/runtime/cli/mod.rs
index a72798d..d115a70 100644
--- a/src/runtime/cli/mod.rs
+++ b/src/runtime/cli/mod.rs
@@ -1093,6 +1093,25 @@ pub mod command {
         Tag::AutoCommand
     }
 
+    /// Publish default runtime options without interpreting the host's argv.
+    /// The context and its log are process-owned: HTTP/HTML services and Worker
+    /// VMs may read them after the session that first initialized Bun retires.
+    pub(crate) fn initialize_embedded_context() {
+        static INITIALIZE: std::sync::Once = std::sync::Once::new();
+        INITIALIZE.call_once(|| {
+            assert!(
+                bun_options_types::context::global_ptr().is_null(),
+                "embedded context initialization must precede CLI publication"
+            );
+            bun_core::set_start_time(bun_core::time::nano_timestamp());
+            // SAFETY: the persistent embedded owner calls this before creating
+            // any VM/Worker. Once performs the only write to the same static
+            // log storage used by CLI startup; it is never dropped per session.
+            let log = unsafe { (*super::cli::LOG_.get()).write(bun_ast::Log::init()) };
+            write_context_no_parse(log);
+        });
+    }
+
     /// Initialize the process-global `CONTEXT_DATA` and publish it via
     /// `Context::set_global`. Shared by `create_context_data` and the
     /// standalone-graph fast path in `start()`.
diff --git a/src/runtime/jsc_hooks.rs b/src/runtime/jsc_hooks.rs
index 248e0bfe..4a3819bc 100644
--- a/src/runtime/jsc_hooks.rs
+++ b/src/runtime/jsc_hooks.rs
@@ -664,7 +664,13 @@ unsafe fn configure_debugger(
 /// # Safety
 /// `state` must be the exact pointer returned by [`init_runtime_state`] for
 /// this thread (or null), and must not be used again after this call.
-unsafe fn deinit_runtime_state(_vm: *mut VirtualMachine, state: OpaqueRuntimeState) {
+unsafe fn deinit_runtime_state(vm: *mut VirtualMachine, state: OpaqueRuntimeState) {
+    // create_node_fs allocates this VM-owned box independently of RuntimeState.
+    // Teardown has waited for every ticket and finalized the JSC heap, so no
+    // native call or JS binding can still borrow it.
+    if let Some(node_fs) = unsafe { (*vm).node_fs.take() } {
+        drop(unsafe { bun_core::heap::take(node_fs.cast::<crate::node::fs::NodeFS>()) });
+    }
     RUNTIME_STATE.with(|c| c.set(ptr::null_mut()));
     // Free the per-thread `TRANSPILE_PRINTER`. Workers lazy-init their own
     // copy in `transpile_file` / `transpile_virtual_module`; without this
@@ -4942,7 +4948,9 @@ pub(crate) fn resolve_embedded_file_to_buf(
     extname: &[u8],
     out_buf: &mut [u8],
 ) -> Option<usize> {
-    if input_path.is_empty() {
+    // A statically embedded host never materializes native graph resources,
+    // including renamed libraries and requests made by Worker VMs.
+    if cfg!(feature = "solid-gpui-embed") || input_path.is_empty() {
         return None;
     }
 
diff --git a/src/runtime/lib.rs b/src/runtime/lib.rs
index b07914c..b2dddfe 100644
--- a/src/runtime/lib.rs
+++ b/src/runtime/lib.rs
@@ -27,6 +27,8 @@ pub mod socket;
 #[path = "webcore.rs"]
 pub mod webcore;
 
+pub mod embedded;
+
 pub mod bake;
 pub mod cli;
 pub mod shell;
diff --git a/src/runtime/node/node_process.rs b/src/runtime/node/node_process.rs
index df6df1bd..8c22e545 100644
--- a/src/runtime/node/node_process.rs
+++ b/src/runtime/node/node_process.rs
@@ -61,6 +61,11 @@ pub(crate) extern "C" fn get_exec_argv(global: &JSGlobalObject) -> JSValue {
     Bun__Process__getExecArgv(global)
 }
 
+/// Private builtin binding: this image owns an application, not Bun's CLI.
+pub(crate) fn is_embedded_host(_global: &JSGlobalObject) -> JSValue {
+    JSValue::from(cfg!(feature = "solid-gpui-embed"))
+}
+
 // ───────────────────────────── exit ─────────────────────────────
 
 // @190n: this may need to be noreturn
@@ -68,7 +73,10 @@ pub(crate) extern "C" fn get_exec_argv(global: &JSGlobalObject) -> JSValue {
 pub(crate) extern "C" fn exit(global_object: &JSGlobalObject, code: u8) {
     let vm = global_object.bun_vm().as_mut();
     vm.exit_handler.exit_code = code;
-    if let Some(worker) = vm.worker_ref() {
+    if vm.is_embedded {
+        // Unwind live JSC frames before the owner destroys this VM.
+        vm.handle().request_termination();
+    } else if let Some(worker) = vm.worker_ref() {
         // @190n: we may need to use requestTerminate or throwTerminationException
         // instead to terminate the worker sooner
         worker.exit();
diff --git a/src/jsc/bindings/JSNodePerformanceHooksHistogram.cpp b/src/jsc/bindings/JSNodePerformanceHooksHistogram.cpp
--- a/src/jsc/bindings/JSNodePerformanceHooksHistogram.cpp
+++ b/src/jsc/bindings/JSNodePerformanceHooksHistogram.cpp
@@ -21,6 +21,7 @@
 #include <JavaScriptCore/HeapAnalyzer.h>
 #include <JavaScriptCore/PropertyName.h>
 #include <chrono>
+#include <limits>
 #include <hdr/hdr_histogram.h>
 #include <wtf/MonotonicTime.h>
 
@@ -184,13 +185,13 @@
 double JSNodePerformanceHooksHistogram::getMean() const
 {
-    if (!m_histogramData.histogram) return NAN;
+    if (!m_histogramData.histogram) return std::numeric_limits<double>::quiet_NaN();
     return hdr_mean(m_histogramData.histogram);
 }
 
 double JSNodePerformanceHooksHistogram::getStddev() const
 {
-    if (!m_histogramData.histogram) return NAN;
+    if (!m_histogramData.histogram) return std::numeric_limits<double>::quiet_NaN();
     return hdr_stddev(m_histogramData.histogram);
 }
 
 int64_t JSNodePerformanceHooksHistogram::getPercentile(double percentile) const
diff --git a/src/js/node/child_process.ts b/src/js/node/child_process.ts
index ed70f054..b0486d7c 100644
--- a/src/js/node/child_process.ts
+++ b/src/js/node/child_process.ts
@@ -781,6 +781,14 @@ function fork(modulePath, args = [], options) {
   options.execPath = options.execPath || process.execPath;
   validateArgumentNullCheck(options.execPath, "options.execPath");
 
+  if (options.execPath === process.execPath && $rust("node_process.rs", "is_embedded_host")) {
+    throw $ERR_INVALID_ARG_VALUE(
+      "options.execPath",
+      options.execPath,
+      "must name a separate JavaScript interpreter; the embedded host has no Bun CLI entry point",
+    );
+  }
+
   // Prepare arguments for fork:
   let execArgv = options.execArgv || process.execArgv;
   validateArgumentsNullCheck(execArgv, "options.execArgv");
diff --git a/src/runtime/Cargo.toml b/src/runtime/Cargo.toml
index 69f0e223..7da16ebc 100644
--- a/src/runtime/Cargo.toml
+++ b/src/runtime/Cargo.toml
@@ -110,3 +110,4 @@ bun_css_jsc.workspace = true
 
 [features]
 default = []
+solid-gpui-embed = []
diff --git a/src/runtime/ffi/ffi_body.rs b/src/runtime/ffi/ffi_body.rs
index 1a1ec085..829e9e59 100644
--- a/src/runtime/ffi/ffi_body.rs
+++ b/src/runtime/ffi/ffi_body.rs
@@ -1457,6 +1457,21 @@ impl FFI {
             return Ok(invalid_options_arg(global));
         };
 
+        if cfg!(feature = "solid-gpui-embed")
+            && bun_options_types::standalone_path::is_bun_standalone_file_path(name_slice.slice())
+        {
+            let error = SystemError {
+                code: bun_core::String::clone_utf8(b"ERR_DLOPEN_FAILED").into(),
+                message: bun_core::String::clone_utf8(
+                    b"Native library extraction is unavailable in an embedded application",
+                )
+                .into(),
+                syscall: bun_core::String::clone_utf8(b"dlopen").into(),
+                ..Default::default()
+            };
+            return Ok(error.to_error_instance(global));
+        }
+
         let mut filepath_buf = bun_paths::path_buffer_pool::get();
         let name: &[u8] = 'brk: {
             let ext: &[u8] = match () {
diff --git a/src/codegen/generate-js2native.ts b/src/codegen/generate-js2native.ts
index a4caebe5..5bec2a52 100644
--- a/src/codegen/generate-js2native.ts
+++ b/src/codegen/generate-js2native.ts
@@ -76,6 +76,8 @@ const rustIdentifierPaths: Record<string, string> = {
   "node_http_binding.rs": "runtime/node/node_http_binding.rs",
   "node_net_binding.rs": "runtime/node/node_net_binding.rs",
   "node_os.rs": "runtime/node/node_os.rs",
+  // The #[path]-mapped Rust module is `node::process`, not `node::node_process`.
+  "node_process.rs": "runtime/node/process.rs",
   "node_quic_binding.rs": "runtime/node/node_quic_binding.rs",
   "node_util_binding.rs": "runtime/node/node_util_binding.rs",
   "node_zlib_binding.rs": "runtime/node/node_zlib_binding.rs",
diff --git a/scripts/build/flags.ts b/scripts/build/flags.ts
index 184aa86f..9d588213 100644
--- a/scripts/build/flags.ts
+++ b/scripts/build/flags.ts
@@ -774,8 +774,8 @@ export const defines: Flag[] = [
   {
     // slash(): path becomes a C string literal — `\U` would be a unicode escape.
     flag: c => `BUN_DYNAMIC_JS_LOAD_PATH=\\"${slash(join(c.buildDir, "js"))}\\"`,
-    when: c => c.debug && !c.ci,
-    desc: "Hot-reload built-in JS from build dir (dev convenience)",
+    when: c => c.debug && !c.ci && c.mode !== "embed-native",
+    desc: "Hot-reload built-in JS for development CLI builds, never embedded products",
   },
   {
     // Standard define that disables assert() in libc headers. CMake adds
@@ -949,9 +949,9 @@
     // the VS-manifest payload xwin downloads carries an ancient arm64 UCRT
     // with broken printf formatting).
     flag: c => quote(`/libpath:${ucrtServicingLibDir(c)!}`, false),
-    when: c => c.windows && c.host.os !== "windows",
-    desc: "Windows cross-compile: serviced Universal CRT static libs (SDK NuGet) override the splat's",
+    when: c => c.windows && c.winsysroot !== undefined,
+    desc: "Windows sysroot: serviced Universal CRT static libs (SDK NuGet) override the splat's",
   },
   {
     // Windows cross-compile: these ldflags go after /link, straight to
     // lld-link, which doesn't see the compile-side `/winsysroot` from
diff --git a/scripts/build/codegen.ts b/scripts/build/codegen.ts
index 7aa6c223..239084a3 100644
--- a/scripts/build/codegen.ts
+++ b/scripts/build/codegen.ts
@@ -743,7 +743,13 @@ function emitJsModules({ n, cfg, sources, o, dirStamp }: Ctx): void {
       desc: "JS modules (bundle-modules)",
       // Note: arg is BUILD_PATH (buildDir), not CODEGEN_PATH. The script
       // derives CODEGEN_DIR = join(BUILD_PATH, "codegen") internally.
-      args: shJoin(cfg, ["run", script, debugFlag(cfg), cfg.buildDir]),
+      args: shJoin(cfg, [
+        "run",
+        script,
+        debugFlag(cfg),
+        cfg.buildDir,
+        ...(cfg.mode === "embed-native" || cfg.ci ? ["--embed-modules"] : []),
+      ]),
     },
   });
 
diff --git a/src/codegen/bundle-modules.ts b/src/codegen/bundle-modules.ts
--- a/src/codegen/bundle-modules.ts
+++ b/src/codegen/bundle-modules.ts
@@ -22,20 +22,21 @@ import { define } from "./replacements";
 
 const BASE = path.join(import.meta.dir, "../js");
 const debug = process.argv[2] === "--debug=ON";
+const embedModules = !debug || process.argv.includes("--embed-modules");
 const CMAKE_BUILD_ROOT = process.argv[3];
 
 const timeString = 'Bundled "src/js" for ' + (debug ? "development" : "production");
 console.time(timeString);
 
 if (!CMAKE_BUILD_ROOT) {
-  console.error("Usage: bun bundle-modules.ts --debug=[OFF|ON] <CMAKE_WORK_DIR>");
+  console.error("Usage: bun bundle-modules.ts --debug=[OFF|ON] <CMAKE_WORK_DIR> [--embed-modules]");
   process.exit(1);
 }
 
 globalThis.CMAKE_BUILD_ROOT = CMAKE_BUILD_ROOT;
 const bundleBuiltinFunctions = require("./bundle-functions").bundleBuiltinFunctions;
 
-const TMP_DIR = path.join(CMAKE_BUILD_ROOT, "tmp_modules");
+const TMP_DIR = path.resolve(CMAKE_BUILD_ROOT, "tmp_modules");
 const CODEGEN_DIR = path.join(CMAKE_BUILD_ROOT, "codegen");
 const JS_DIR = path.join(CMAKE_BUILD_ROOT, "js");
 
@@ -211,8 +212,9 @@
 // directory caching stuff breaks this sometimes. CLI rules
+// Relative inputs avoid repeating the build root beyond Windows' command-line limit.
 const config_cli = [
   process.execPath,
   "build",
-  ...bundledEntryPoints,
+  ...bundledEntryPoints.map(entrypoint => "./" + path.relative(TMP_DIR, entrypoint)),
   ...(debug ? [] : ["--minify-syntax", "--keep-names"]),
   "--root",
   TMP_DIR,
@@ -232,7 +234,7 @@
 verbose("running: ", config_cli);
 const out = Bun.spawnSync({
   cmd: config_cli,
-  cwd: process.cwd(),
+  cwd: TMP_DIR,
   env: process.env,
   stdio: ["pipe", "pipe", "pipe"],
 });
@@ -407,8 +409,8 @@ writeIfNotChanged(
 // WebCoreJSBuiltins.cpp's internalCombinedSource is the span at offset 0; the
 // internal modules follow at known offsets.
 //
-// In debug builds the module sources are read from disk (BUN_DYNAMIC_JS_LOAD_PATH),
-// so every module offset/length is 0. The functions span is still real in debug.
+// Development CLI builds may read module sources from disk. Embedded and CI
+// products must carry real module spans even when debug assertions are enabled.
 const moduleSpans: { enumName: string; offset: number; length: number }[] = [];
 let blob: Buffer;
 {
@@ -416,7 +418,7 @@ let blob: Buffer;
   let offset = chunks[0].length;
   for (const id of layoutOrder(moduleList.slice(0, nativeStartIndex), outputs)) {
     const enumName = idToEnumName(id);
-    if (debug) {
+    if (!embedModules) {
       moduleSpans.push({ enumName, offset: 0, length: 0 });
       continue;
     }
diff --git a/src/jsc/web_worker.rs b/src/jsc/web_worker.rs
index 3778bc99..42f6e900 100644
--- a/src/jsc/web_worker.rs
+++ b/src/jsc/web_worker.rs
@@ -1294,8 +1294,9 @@ unsafe fn resolve_entry_point_specifier<'s>(
 ) -> Option<&'s [u8]> {
     // SAFETY: per fn contract; read-only field.
     if let Some(graph) = unsafe { (*parent).standalone_module_graph } {
-        if graph.find(str).is_some() {
-            return Some(str);
+        if let Some(canonical_path) = graph.find(str) {
+            // Native Windows separators must not escape into the module loader.
+            return Some(canonical_path);
         }
 
         // Since `bun build --compile` renames files to `.js` by default, we
diff --git a/scripts/build/winsysroot.ts b/scripts/build/winsysroot.ts
--- a/scripts/build/winsysroot.ts
+++ b/scripts/build/winsysroot.ts
@@ -173,10 +173,10 @@
  * the case aliases the LLVM toolchain needs. Fetches the sysroot with xwin
  * when it's missing — CI only; local builds get a clear error instead of a
  * surprise multi-GB download into a directory they configured themselves.
- * No-op for native Windows builds.
+ * Native Windows uses this path when a sysroot was explicitly selected.
  */
 export async function ensureWindowsSysroot(cfg: Config): Promise<void> {
-  if (!cfg.windows || cfg.host.os === "windows" || cfg.winsysroot === undefined) return;
+  if (!cfg.windows || cfg.winsysroot === undefined) return;
   const dest = cfg.winsysroot;
 
   if (!isCompleteWindowsSysroot(dest, cfg.arch)) {
@@ -198,12 +198,11 @@
  * Directory holding the serviced UCRT static libs for the target arch (see
  * UCRT_SERVICING_VERSION). The link adds it as /libpath: ahead of the
- * winsysroot so these win over the splat's stale copies. Undefined for
- * native-Windows builds (they link the locally installed, already-serviced
- * SDK).
+ * winsysroot so these win over the splat's stale copies. Undefined when
+ * the native Windows SDK is used without an explicit sysroot.
  */
 export function ucrtServicingLibDir(cfg: Config): string | undefined {
-  if (!cfg.windows || cfg.host.os === "windows") return undefined;
+  if (!cfg.windows || cfg.winsysroot === undefined) return undefined;
   return join(cfg.cacheDir, `ucrt-servicing-${UCRT_SERVICING_VERSION}`, msArchName(cfg.arch));
 }
 
 /** Fetch the serviced UCRT libs from the Microsoft.Windows.SDK.CPP NuGet. */

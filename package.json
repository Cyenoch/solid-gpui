{
  "name": "solid-gpui-workspace",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.2",
  "engines": {
    "bun": ">=1.4.2"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "task": "bun scripts/tasks.ts",
    "build": "bun run task build",
    "format": "bun run task format",
    "check": "bun run task check",
    "test": "bun run task test",
    "ci": "bun run task ci",
    "audit": "bun run task audit",
    "gallery": "bun run task gallery",
    "gallery:vite": "bun run --cwd examples/gallery-vite dev",
    "gallery:package": "bun run task gallery-package"
  },
  "devDependencies": {
    "@jridgewell/remapping": "2.3.5",
    "@solidjs/compiler": "2.0.0-rc.6",
    "bebop-tools": "3.2.3",
    "bun-types": "1.4.2",
    "commander": "15.0.0",
    "oxc-transform": "0.148.0",
    "oxfmt": "0.66.0",
    "solid-js": "1.9.15",
    "typescript": "7.0.2"
  },
  "trustedDependencies": [
    "bebop-tools"
  ]
}

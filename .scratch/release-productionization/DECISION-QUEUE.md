# Release decision queue

**Date:** 2026-08-31  
**Evidence baseline:** `HEAD 54c5993` (before this queue commit)

The project is production-ready internally: the current contract has no implementation backlog, the release gates and package/archive checks have passed, and `v0.2.0` is cut and tagged locally. It is not yet externally distributable: publication is still held at the narrowed license boundary, while push, npm publication, signing/notarization, and the next cut await human choices. From here, the remaining value is in user-owned actions—upstream communication, legal/account credentials, release authority, and the optional live-process restart—not more agent-side implementation. Every row below links its source evidence.

## Decisions, in leverage order

| ID | What it is / why it is yours | What it unlocks | Effort | Exact next step / artifact | Source |
| --- | --- | --- | --- | --- | --- |
| **D1 — Post upstream license issue 01** | Copy-paste the prepared Zed issue asking for `license`/`license-file` metadata on `gpui_shared_string` and `gpui_util`. This is yours because only the upstream-facing account and maintainer communication can request a future release change; the issue deliberately does not choose a license for the copyright holders. **Issue 02 is optional** and should be ordered deliberately with D1 because it discusses the private downstream context. | **Unblocks the last hard blocker’s fix path:** an upstream answer/fix that can arrive in a future Zed release. It does not itself constitute legal clearance or publish authorization. | **10 min** after the pre-post checklist. | Open [`upstream/01-license-fields.md`](../license-audit/upstream/01-license-fields.md), verify the two manifests/dependency lines and recapture the exact `cargo deny` excerpt, remove private details, then post the supplied title/body in `zed-industries/zed`. If also posting optional issue 02, decide whether it precedes or follows this issue. | [`01-license-fields.md`](../license-audit/upstream/01-license-fields.md), especially its posting checklist and suggested fix; optional [`02-apache-gpui-gpl-ztracing.md`](../license-audit/upstream/02-apache-gpui-gpl-ztracing.md) |
| **D2 — Confirm the remaining license items with legal** | Obtain a written determination for `self_cell`’s Apache option, MPL-2.0 file-scope/notice obligations (and other weak-copyleft conditions as applicable), and the exact host/npm artifact treatment. This is yours because it is a legal judgment and may require counsel or rights-holder/account records; agents must not infer or select terms. | **Closes publication legality alongside D1’s upstream path** and supplies the written basis to remove the STOP, produce notices/source offers/corresponding-source or relinkable-object materials, and re-audit the exact artifacts. | **Legal review session.** | Attach the written determination and artifact-level re-audit to [`license-audit/issues/01-gpl-and-unknown-host-licenses.md`](../license-audit/issues/01-gpl-and-unknown-host-licenses.md); review the exact source revision, host archive, and each npm tarball intended for distribution before changing the STOP. | [`license-audit/2026-08-30.md`](../license-audit/2026-08-30.md#release-action), [`issues/01-gpl-and-unknown-host-licenses.md`](../license-audit/issues/01-gpl-and-unknown-host-licenses.md#required-release-owner-checklist) |
| **D3 — Push the repository and tag** | Push `main` and the annotated `v0.2.0` tag. This is yours because the remote, ordering, and disclosure boundary are account/repository decisions; `origin` is configured and the local history is already 152+ commits with the local tag. **Note:** optional upstream issue 02 says the downstream repository is private—decide its posting/push ordering with D1 before exposing anything. | **Starts first cross-platform runner validation and puts CI live**; it also makes the approved tag reachable to the release workflows. | **One command + review.** | After D1/D2’s disclosure and legality decisions, review the final tree, then run `git push origin main v0.2.0`; capture the Linux/Windows workflow results and pushed tag. | [`release-productionization/release-artifacts.md`](release-artifacts.md#push-day-checklist-stub), [`release-productionization/issues/06-cross-platform-host.md`](issues/06-cross-platform-host.md), and optional issue 02’s privacy checklist [`license-audit/upstream/02-apache-gpui-gpl-ztracing.md`](../license-audit/upstream/02-apache-gpui-gpl-ztracing.md) |
| **D4 — Choose and execute npm publication policy** | Decide registry, package access, first-publication policy for `@react-gpui/core` and `@react-gpui/dev`, and provide a short-lived token with the required scope. This is yours because it requires an npm account, package ownership/access, and a secret; no token or publish command is stored in the repository. | **Unlocks the `bun add` onboarding path** (`bun add react @react-gpui/core`) in its elegant public form, rather than the current checkout/tarball path. | **Account + token + command.** | Verify the candidate metadata and tarballs, then from each package directory run the approved `npm publish` command (for a first public scoped package, normally `npm publish --access public`) using the short-lived token; publish only the packages explicitly approved. | [`release-artifacts.md`](release-artifacts.md#push-day-checklist-stub), [`packages/react-gpui/package.json`](../../packages/react-gpui/package.json), [`packages/react-gpui-dev/package.json`](../../packages/react-gpui-dev/package.json), and the no-publish candidate workflow [`../../.github/workflows/release-prep.yml`](../../.github/workflows/release-prep.yml#L45-L63) |
| **D5 — Cut 0.3.0** | Move the matured `Unreleased` entries into a `0.3.0` section and perform the next cut. The current cycle is **6 Added / 6 Testing / 5 Fixed / 3 Changed**; the breaking `PointerAction` removal makes 0.3.0 the documented recommendation, not 0.2.1. This is yours because version/date/changelog/tag approval belongs to the release owner. | **Unlocks the release cadence** and creates the next coherent version for the four synchronized sources. Recommendation: do this **after D1–D4**. | **One command + gates.** | In a release worktree, create the `## [0.3.0]` changelog section, run `make release-prep VERSION=0.3.0`, then run the approved gates (`make ci`, `make embedded-bun`, `make host-candidate-smoke`, `make host-embedded-candidate-smoke`, and `make bun-pack-smoke`) before committing/tagging. | [`rehearsal-2/notes.md`](../rehearsal-2/notes.md) (four-source sync and deterministic archive), [`release-readiness.md`](release-readiness.md#known-limits-and-human-decision-items), [`v0.2.0-cut-checklist.md`](v0.2.0-cut-checklist.md#human-decisions-and-publication-boundaries) |
| **D6 — Sign/notarize, or explicitly accept unsigned** | Choose the authenticated host-artifact path (signing/notarization credentials and process) or explicitly accept the documented unsigned candidate limitation. This is yours because the credentials, publisher identity, and risk acceptance are human-owned. | **Unlocks an authenticated host distribution**; explicit acceptance instead unlocks a deliberate unsigned release posture without pretending checksums authenticate the publisher. | **One release-process decision.** | Resolve [`issues/01-unsigned-notarization.md`](issues/01-unsigned-notarization.md): if approved, run the configured signing/notarization process and replace or supplement the unsigned archive; if accepted, record the written acceptance and retain the limitation in release notes. | [`issues/01-unsigned-notarization.md`](issues/01-unsigned-notarization.md), [`release-artifacts.md`](release-artifacts.md#push-day-checklist-stub) |
| **D7 — Optional: restart the live gallery** | Restart the locally running gallery so it loads the current checkout’s 30+ commits of features (rich text, cursor rule, error messages, `skrifa`). This is yours because it is your live process and its state; agents never touched or restarted it. | **Makes the current feature set visible in the process you are actually using**; it changes no release artifact or source. | **A few minutes.** | Stop the existing gallery process and relaunch the documented embedded gallery command: `cargo run -p react-gpui-host --features embedded-bun -- --runtime embedded packages/react-gpui/examples/gallery.tsx`. Do not start a second copy while the old one is live. | [`README.md`](../../README.md#gallery-embedded-bunjsc), [`CHANGELOG.md`](../../CHANGELOG.md#L15-L47), and the live-process boundary in [`examples-smoke/notes.md`](../examples-smoke/notes.md) |

## If you do nothing else

**Do D1 first.** Posting the prepared upstream issue is the single highest-leverage action: it unblocks the only hard blocker’s fix path, while D2–D7 are parallel human decisions or follow-on execution once disclosure and legality are settled.

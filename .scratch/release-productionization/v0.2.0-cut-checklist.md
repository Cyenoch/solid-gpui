# v0.2.0 cut checklist

Status: **0.2.0 cut executed locally**; publication remains pending. No version,
tag, or changelog migration remains pending. This refresh records the new cycle
after the local cut.
Evidence release commit: `72a520c` (`chore(release): 0.2.0`). Annotated local tag:
`v0.2.0` (tag object `018f7f85e1c8aa756df14facdd7d7ff761fba683`).

## Cut execution record

- **Executed:** `2026-08-30`
- **Release commit:** `72a520c` (`chore(release): 0.2.0`)
- **Annotated local tag:** `v0.2.0`, pointing to `72a520c`; no push or other remote operation.
- **Changelog:** every former Unreleased entry moved under `## [0.2.0] - 2026-08-30`; the new cycle now has exactly **2 Added / 1 Fixed / 0 Changed** entries.
- **Version/lock scope:** workspace Cargo, the three workspace package lines in `Cargo.lock`, and both package manifests synchronized to `0.2.0`; both Bun lockfiles were unchanged.
- **Automation deviation:** the dated-heading guard in `scripts/release-prep.sh` was not broadened. The checklist-approved temporary exact `## [0.2.0]` heading was used for prep, then the dated heading was finalized.
- **Publication boundaries:** no push, registry publish, signing, or notarization was performed. User-owned `AGENTS.md` remained unstaged and untouched.

## Fresh post-cut five-gate matrix

All five gate outcomes below exited 0 and ran serially/exclusively under
`/usr/bin/time -p` on current HEAD `9e53ea9`. Candidate timeout lines are
expected successful behavior.

| Command | Outcome | Real time | Evidence |
| --- | ---: | ---: | --- |
| `make ci` | 0 | **79.28 s** | Rust **128 passed / 0 failed**; Core Bun **130 pass / 0 fail / 90,533 expect() calls**; dev Bun **24 pass / 0 fail / 56 expect() calls**; formatting/checks/builds/typechecks and package consumer smoke passed. |
| `make embedded-bun` | 0 | **3.97 s** | `embedded_examples` **2 passed**; embedded counter **1 passed**; locked checks passed. |
| `make host-candidate-smoke` | 0 | **35.05 s** | Identical archive SHA-256 **`d3d3075697876f6a0d1055251693d5b5f5b03b9c26f969655729af7999478c3c`**; archive `react-gpui-host-0.2.0-aarch64-apple-darwin.tar.gz`; snapshot commit **312 bytes**; expected process timeouts **5.019 s/5.013 s**; version/help passed. |
| `make host-embedded-candidate-smoke` | 0 | **19.85 s** | Release embedded binary; `--smoke-press` reported `sent=true, commits=2, status=None`; expected timeout **4.795 s**; version/help passed. |
| `make bun-pack-smoke` | 0 | **2.03 s** | Frozen installs, both 0.2.0 builds/tarballs, external consumer install (**59 packages**), and consumer smoke passed. |

Successful post-cut gate wall-time sum: **140.18 s**. The current candidate
archive is version `0.2.0`; candidate timeout exits are expected behavior.

## Historical preparation-pass gate matrix

The preparation-pass matrix was green at HEAD `7edad1f`; it is retained for rehearsal provenance and is superseded by the final cut matrix above. The smoke-hang fix `f34ccfb` root-caused the harness flake and bounded build/teardown waits.

| Command | Exit | Real time | Evidence |
| --- | ---: | ---: | --- |
| `make ci` | 0 | **35.89 s** | Rust **126 passed / 0 failed**; Core Bun **130 pass / 0 fail / 89,335 expect() calls**; dev Bun **24 pass / 0 fail / 56 expect() calls**; builds, typechecks, package consumer smoke, and TextInput typing-performance guard passed. |
| `make embedded-bun` | 0 | **1.72 s** | `embedded_examples` **2 passed**; embedded counter **1 passed**. |
| `make host-candidate-smoke` | 0 | **34.60 s** | Identical archive SHA-256 **`fc9a926fb65163d748bd4059e43305857f0e704b3324ca23057814de1890bc58`**; snapshot commit **312 bytes**; expected process timeouts **5.018 s/5.022 s**; version/help passed. |
| `make host-embedded-candidate-smoke` | 0 | **19.95 s** | Release embedded binary; `--smoke-press` reported `sent=true, commits=2, status=None`; expected timeout **4.811 s**; version/help passed. |
| `make bun-pack-smoke` | 0 | **2.22 s** | Frozen installs, both builds/tarballs, external consumer install (**59 packages**), and consumer smoke passed. |

Successful recorded-run wall-time sum: **94.38 s**. Candidate timeout lines were expected successful rehearsal behavior.


## Mechanical cut sequence (executed)

1. [x] Moved the former Unreleased content under `## [0.2.0] - 2026-08-30`
   and left an empty `## [Unreleased]` section. The dated-heading guard was
   handled with the temporary exact-heading deviation recorded above.
2. [x] Ran `make release-prep VERSION=0.2.0`; reviewed manifest and lockfile
   diffs.
3. [x] Ran the five release gates serially and exclusively before the release
   commit/tag; the embedded candidate required one clean retry after a
   transient TLS failure in the original rehearsal.
4. [x] Reviewed the dated changelog, generated locks, archive name, and
   `git diff --check`; committed the version cut as `72a520c`.
5. [x] Created the local annotated tag `v0.2.0` after all five pre-cut gate
   outcomes passed. No push or publication was performed.

## Post-cut refresh record

- **Current HEAD at refresh:** `9e53ea9` (`perf(renderer): affected-aware
  reconciliation and snapshot guard`).
- **Current Unreleased:** exactly **2 Added / 1 Fixed / 0 Changed**. The
  gallery rich-text card is Added, the large-tree guard is Added, and the
  affected-aware reconciliation is Fixed. The gallery classification was
  corrected after the prior refresh; this is the fourth section-placement
  correction called out in this evidence series.
- **Post-cut commits:** `f48f91e`, `ddb21bf`, `593ab52`, `38a0f2d`, `ea6b67b`,
  `c01adf5`, and `9e53ea9`; the readiness report contains the full crosswalk.
- **Fuzz:** deterministic coverage is complete at **35/35 command seeds and
  23/23 event seeds** in both Rust and TypeScript records.
- **Gallery/troubleshooting:** the runnable rich-text/link/selectable gallery
  showcase and symptom→diagnosis→repair troubleshooting guide are present.
- **Affected reconciliation/performance:** the guard covers 1k/5k/20k-node
  trees and a one-operation 20k patch. Measured 20k host stages are **70.705
  ms** for snapshot and **0.131 ms** for the one-op patch; apply p50/p99 is
  **259.019/259.656 ms**, first-draw p50/p99 **195.747/198.639 ms**.
- **GPUI drift:** verdict is **no released unblocks** for the recorded upstream
  boundaries.
- **Candidate archive comparison:** the pre-cut SHA was
  `d0dfc86e8c5b98d790d99eae0ec2ebb485e3b1a8cc28939cdafbf9a973e25ab`; the
  post-cut SHA is
  `d3d3075697876f6a0d1055251693d5b5f5b03b9c26f969655729af7999478c3c`.
  They differ because `9e53ea9` changed the host binary. The tag still points
  at `72a520c`; do not move it.

## Human decisions and publication boundaries

- [x] Approve release version `0.2.0` and release date `2026-08-30` for the
  local cut.
- [ ] Resolve issue `01-unsigned-notarization`: provide signing/notarization
  credentials/process or explicitly accept the unsigned candidate and its
  documented limitation.
- [x] Approve tag convention `v0.2.0`; the annotated tag was created locally
  only after all five pre-cut gate outcomes passed.
- [ ] Decide npm registry, package access, first-publication policy for core
  and dev, and provide a short-lived npm token with required scope. No npm
  publish command or secret exists in this repository.
- [ ] Decide whether any Rust crate is published separately; this preparation
  does not imply crates.io publication.

**Current position:** v0.2.0 cut locally + tagged, publication
(push/npm/signing) pending human action. No push, registry publish, signing,
or notarization was performed in this refresh. User-owned `AGENTS.md` remains
unstaged and untouched.

This file is local evidence under `/.scratch/`, ignored by `.gitignore` by
default. The readiness report and checklist are force-added under the
repository's evidence convention; neither is a published artifact. No option
is selected by this checklist.

# v0.2.0 cut checklist

Status: **0.2.0 cut executed locally**; publication remains pending. No version,
tag, or changelog migration remains pending. This refresh records the post-cut
cycle at current HEAD.

Evidence release commit: `72a520c` (`chore(release): 0.2.0`). Annotated local tag:
`v0.2.0` (tag object `018f7f85e1c8aa756df14facdd7d7ff761fba683`).

## Cut execution record

- **Executed:** `2026-08-30`
- **Release commit:** `72a520c` (`chore(release): 0.2.0`)
- **Annotated local tag:** `v0.2.0`, pointing to `72a520c`; no push or other remote operation.
- **Changelog:** every former Unreleased entry moved under `## [0.2.0] - 2026-08-30`; the current cycle now has **6 Added capability / 5 Testing / 3 Fixed / 2 Changed** entries at final HEAD `d74467e`.
- **Version/lock scope:** workspace Cargo, the three workspace package lines in `Cargo.lock`, and both package manifests synchronized to `0.2.0`; both Bun lockfiles were unchanged by the cut.
- **Publication boundaries:** no push, registry publish, signing, or notarization was performed. User-owned `AGENTS.md` remains unstaged and untouched.

## Fresh readiness refresh #11

- **Timestamp:** `2026-08-30T12:26:13+0800`.
- **Current HEAD:** `d74467e` (`build(ci): clarify transitive font advisory`).
- **Refresh delta from `c7cb1a0`:** 5 commits: `0b88b2b`, `c5cc405`, `f7bc498`, `8626ec2`, and `d74467e`.
- **Current Unreleased:** **6 Added capability / 5 Testing / 3 Fixed / 2 Changed**; release-facing count is **11 additions / 3 fixes / 2 changes** when Testing is counted with additions.
- **Section accounting:** the audit sweep's `@babel/core` bump is verified in `package.json`/`bun.lock` and supply-chain evidence, but did not add a current Unreleased bullet; the audit gate is the new Added bullet, and skrifa is the new Changed bullet. Counts above are exact section counts, not inferred classifications.
- **Capabilities:** examples launch smoke **14/14**, process kill resilience six scenarios with zero orphans, headless last-surface lifecycle coverage, renderer soak plateau, TypeScript commit-emission property invariants, public export audit/follow-ups, verified standalone distribution pair, onboarding dogfood, actionable diagnostics, contributor guide, documentation index, shared multi-surface example, advisory scan/gate wiring, and direct font-parser dependency elimination.
- **Property and soak evidence:** surface 16 seeds x 48 operations; tree 32 seeds x 64 operations; TypeScript 24 seeds x 40 mutations; renderer soak 2,000 iterations with flat post-warm-up RSS/side maps; host lifecycle six-scenario verdict clean. All focused evidence is covered by `make ci` except the standalone display-boundary smokes.
- **Fuzz:** deterministic protocol coverage remains **35/35 command seeds and 23/23 event seeds** in both Rust and TypeScript records.
- **Supply-chain boundary:** scan -> gate -> direct-dependency elimination is now explicit. Bun audit is clean after `@babel/core` **7.28.4 -> 7.29.6`; `make ci` runs both Bun audits plus cargo-deny; `skrifa` replaces the application-owned direct `ttf-parser` edge. Transitive `ttf-parser` remains in the pinned GPUI/fontdb/rustybuzz graph and stays acknowledged in `deny.toml`; no global advisory closure is claimed. `cargo-audit` is unavailable.

## Fresh five-gate matrix

All five commands exited 0 and ran serially/exclusively under `/usr/bin/time -p`
at candidate HEAD `d74467e`, in the order shown below. Candidate timeout exits are
expected successful rehearsal behavior. The `make ci` output shape now includes
its advisory step after the existing Bun gates: `bun audit` prints
`No vulnerabilities found (checked 12 packages)` and
`No vulnerabilities found (checked 59 packages)`, then Make prints
`==> cargo deny check advisories` and cargo-deny prints `advisories ok`.

| Command | Outcome | Real time | Evidence |
| --- | ---: | ---: | --- |
| `make ci` | 0 | **94.81 s** | Rust workspace green; Core Bun **132 pass / 0 fail / 453,263 expect() calls**; dev Bun **24 pass / 0 fail / 56 expect() calls**; formatting, checks, builds, typechecks, package smoke, property, soak, performance guards, both Bun audits (12 and 59 packages, no vulnerabilities), and cargo-deny (`advisories ok`) passed. |
| `make embedded-bun` | 0 | **4.37 s** | `embedded_examples` **2 passed**; embedded counter **1 passed**; locked checks passed. |
| `make host-candidate-smoke` | 0 | **55.00 s** | Identical archive SHA-256 **`abb0aeb784d99d9761636d128bb104331f934bf1378ae72b31b311d1092ccdef`** matched on both archives; archive `react-gpui-host-0.2.0-aarch64-apple-darwin.tar.gz`; snapshot commit **312 bytes**; expected process timeouts **5.019 s/5.013 s**; version/help passed. |
| `make host-embedded-candidate-smoke` | 0 | **20.26 s** | Release embedded binary; `--smoke-press` reported `sent=true, commits=2, status=None`; expected timeout **4.820 s**; version/help passed. |
| `make bun-pack-smoke` | 0 | **3.57 s** | Frozen installs, both 0.2.0 JS/type builds and tarballs, external consumer install (**59 packages**), and `package tarball consumer smoke passed`. |

Successful sequential five-gate wall-time sum: **178.01 s**. Candidate timeout
lines are expected behavior, not gate failures. The archive SHA is the
process-candidate check's identical pair of SHA-256 values.

## Capability notes

- **Examples launch smoke (`fe0af9e`, `53a895e`):** launched **14/14** runnable examples via process mode, emitted `protocol=v3`, and tore down cleanly; the target does not touch live gallery processes and the package example table is complete.
- **Process kill resilience (`9fd771e`, `370684e`):** covered host PID, renderer PID, and process-group SIGKILL for counter and gallery: six scenarios, **zero orphans**, stdin-EOF renderer exit, and typed child-death diagnostics.
- **Last-surface lifecycle (`3a1a9ea`):** passed the focused headless test: six close-policy/lifecycle scenarios are clean; first close leaves the host alive and final close requests application quit after the registry empties. Native user-close process exit timing is display-backed and not claimed.
- **Renderer soak (`486fb71`):** remains plateau/flat after warm-up; RSS rose 608 KiB across 2,000 deterministic iterations.
- **TypeScript emission property (`60b3ddf`):** covers 24 seeds x 40 mutations, 960 post-bootstrap commits plus 24 snapshots, and 362,716 targeted assertions.
- **Export audit (`f0e725b`, `a32b53d`):** leaves checked-in API locks core 115/dev 20 after removing dead `PointerAction`; future-surface follow-ups remain documented.
- **Supply-chain:** `.scratch/supply-chain/2026-08-30.md` records the advisory scan and Babel remediation; `.scratch/audit-wiring/notes.md` records the strict Bun audit/cargo-deny gate; `.scratch/skrifa-spike/notes.md` records the direct parser-edge elimination and transitive upstream boundary.

## Crosswalk from `c7cb1a0`

| Commit | Reachable evidence |
| --- | --- |
| `0b88b2b` | Security audit sweep: Bun advisory scan, `@babel/core` **7.28.4 -> 7.29.6**, and three tracked Rust advisory issues in `.scratch/supply-chain/`; no current Unreleased changelog bullet. |
| `c5cc405` | Refreshed `.scratch/EVIDENCE.md` to index **127 tracked files across 51 evidence areas**; also landed the current Unreleased audit Added and skrifa Changed bullets. |
| `f7bc498` | Added strict `make audit` and wired it into `make ci`; current Unreleased **Added** bullet; `deny.toml` acknowledges the three tracked unmaintained advisories. |
| `8626ec2` | Migrated the application-owned font-family parser to `skrifa`; current Unreleased **Changed** bullet; focused malformed/Tuffy tests and supply-chain issue update. |
| `d74467e` | Corrected deny-policy wording to state direct `ttf-parser` elimination while transitive pinned-GPUI/fontdb occurrences remain; no changelog section change. |

## Mechanical cut sequence (executed)

1. [x] Moved the former Unreleased content under `## [0.2.0] - 2026-08-30` and left an empty `## [Unreleased]` section.
2. [x] Ran `make release-prep VERSION=0.2.0`; reviewed manifest and lockfile diffs.
3. [x] Ran the five release gates serially and exclusively before the release commit/tag.
4. [x] Reviewed the dated changelog, generated locks, archive name, and `git diff --check`; committed the version cut as `72a520c`.
5. [x] Created the local annotated tag `v0.2.0` after all five pre-cut gate outcomes passed. No push or publication was performed.

## Human decisions and publication boundaries

- [x] Approve release version `0.2.0` and release date `2026-08-30` for the local cut.
- [ ] Resolve issue `01-unsigned-notarization`: provide signing/notarization credentials/process or explicitly accept the unsigned candidate and its documented limitation.
- [x] Approve tag convention `v0.2.0`; the annotated tag was created locally only after all five pre-cut gate outcomes passed.
- [ ] Decide npm registry, package access, first-publication policy for core and dev, and provide a short-lived npm token with required scope. No npm publish command or secret exists in this repository.
- [ ] Decide whether any Rust crate is published separately; this preparation does not imply crates.io publication.
- **Recommendation only:** because Unreleased has matured and includes the breaking `PointerAction` removal, the next cut should be **0.3.0**, not **0.2.1**, under pre-1.0 SemVer; no version is selected.

**Current position:** v0.2.0 cut locally + tagged, publication (push/npm/signing)
pending human action. No push, registry publish, signing, or notarization was
performed in this refresh. User-owned `AGENTS.md` remains unstaged and untouched.

This file is local evidence under `/.scratch/`, ignored by `.gitignore` by
default. The readiness report and checklist follow the repository's force-add
evidence convention; neither is a published artifact. No publication option is
selected by this checklist.

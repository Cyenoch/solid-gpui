# v0.2.0 cut checklist

Status: **0.2.0 cut executed locally**; publication remains pending. No version,
tag, or changelog migration remains pending. This refresh records the post-cut
cycle at current HEAD.

Evidence release commit: `72a520c` (`chore(release): 0.2.0`). Annotated local tag:
`v0.2.0` (tag object `018f7f85e1c8aa756df14facdd7d7ff761fba683`).

## Cut execution record

- **Executed:** `2026-08-30`
- **Release commit:** `72a520c` (`chore(release): 0.2.0`)
- **Annotated local tag:** `v0.2.0`, pointing to `72a520c`; no push or other remote operation.
- **Changelog:** every former Unreleased entry moved under `## [0.2.0] - 2026-08-30`; the current cycle now has **6 Added capability / 6 Testing / 5 Fixed / 3 Changed** entries at refresh HEAD `9ac496a`.
- **Version/lock scope:** workspace Cargo, the three workspace package lines in `Cargo.lock`, and both package manifests synchronized to `0.2.0`; both Bun lockfiles were unchanged by the cut.
- **Publication boundaries:** no push, registry publish, signing, or notarization was performed. User-owned `AGENTS.md` remains unstaged and untouched.

## Fresh readiness refresh #14

- **Timestamp:** `2026-08-31T06:42:21+0800`.
- **Current HEAD:** `9ac496a` (`build(deps): replace ztracing with local no-op stub (option 3′)`).
- **Refresh delta from `d8608e2`:** 2 commits: `5c29636` and `9ac496a`.
- **Current Unreleased:** **6 Added capability / 6 Testing / 5 Fixed / 3
  Changed**; release-facing count is **12 additions / 5 fixes / 3 changes**.
- **Section accounting:** the two commits add the Option 3′ proof and its
  executed landing; the landing also adds the one requested `Changed` entry to
  `CHANGELOG.md`. Counts are exact section counts, not inferred
  classifications.
- **License decision status:** **STOP narrowed, still ready-for-human.** The
  GPL trio (`zlog`, `ztracing`, and `ztracing_macro`) is eliminated from the
  distributed host graph by executed Option 3′. The remaining STOP covers
  `gpui_shared_string`/`gpui_util` missing license fields, `self_cell`'s
  dual-license confirmation, weak-copyleft notices, and artifact-level legal
  review. Publication remains blocked; this is a legal decision, not an
  engineering waiver.
- **Option matrix:** Options 1 and 2 remain evidence-dead; **Option 3′ is
  EXECUTED**, reversible, and its README documents restoring the upstream
  dependency; Option 4 remains legal-only and is not selected.

## Fresh five-gate matrix

All five commands exited 0 and ran serially/exclusively under `/usr/bin/time -p`
at candidate HEAD `9ac496a`:

| Command | Outcome | Real time | Evidence |
| --- | ---: | ---: | --- |
| `make ci` | 0 | **95.77 s** | Core Bun **132/132**, **453,263 assertions**; dev Bun **24/24**, **56 assertions**; Rust, builds, typechecks, package smoke, both Bun audits (12/59 packages), and cargo-deny (`advisories ok`) passed. |
| `make embedded-bun` | 0 | **4.29 s** | `embedded_examples` **2 passed**; embedded counter **1 passed**; locked checks passed. |
| `make host-candidate-smoke` | 0 | **49.20 s** | Identical archive SHA-256 **`bf737394a9a027a50edbd77fa4e55319ba22bda2ee740a8966961f62a6b7805f`**; 312-byte snapshot; expected process timeouts **5.015 s / 5.020 s**; version/help passed. |
| `make host-embedded-candidate-smoke` | 0 | **19.68 s** | `--smoke-press`: `sent=true, commits=2, status=None`; expected timeout **4.569 s**; version/help passed. |
| `make bun-pack-smoke` | 0 | **2.13 s** | Frozen installs, both 0.2.0 JS/type builds and tarballs, external consumer install (**59 packages**), and package tarball consumer smoke passed. |

Successful sequential five-gate wall-time sum: **171.07 s**. Candidate timeout
exits are expected successful rehearsal behavior.

## Capability and license notes

- **Option 3′ proof and landing:** `5c29636` proves the independently authored
  Apache-2.0 identity proc-macro stub and target graph result; `9ac496a` lands
  the Cargo patch, lockfile result, README reversibility note, and narrowed
  STOP. No Zed tracing implementation code was copied.
- **License check movement:** informational `cargo deny check licenses` remains
  red under the strict empty allow-list policy, but rejected records moved
  **670 → 663** and GPL package rejection blocks moved **3 → 0**. The same two
  missing-license-field findings remain, with one local Apache-2.0 stub block.
- **Human publication boundary:** the release owner and legal reviewer must
  still resolve the two missing fields, `self_cell` dual-license option,
  weak-copyleft notices/obligations, and exact artifact-level legal review.

## Crosswalk from `d8608e2`

| Commit | Reachable evidence |
| --- | --- |
| `5c29636` | Option 3′ stub proof in `.scratch/license-audit/spike-option3-stub.md`, including identity-macro semantics, target graph checks, and gate evidence. |
| `9ac496a` | Option 3′ executed landing in `.scratch/license-audit/landing-option3.md`, plus the Cargo patch, lockfile result, changelog entry, and narrowed STOP recorded in issue 01. |

## Human decisions and publication boundaries
> **STOP (narrowed) — LICENSE BLOCKER:** Do not push, publish, sign, notarize, or otherwise distribute this cut until the release owner and a legal reviewer resolve [license audit issue 01](../license-audit/issues/01-gpl-and-unknown-host-licenses.md). Option 3′ has eliminated the GPL trio from the distributed host graph. The remaining STOP covers `gpui_shared_string`/`gpui_util` missing license fields, `self_cell` dual-license confirmation, weak-copyleft notices, and artifact-level legal review. Publication is a legal decision, not an engineering waiver.

- [x] Existing local `0.2.0` cut/tag remains approved and unchanged.
- [x] Option 3′ local no-op stub landed; the matrix status is **EXECUTED**
  and the change is reversible through the README procedure.
- [ ] Resolve the narrowed license blocker: written legal determination for
  both missing license fields, `self_cell`'s permitted option, weak-copyleft
  conditions/notices, and exact artifact-level re-audit.
- [ ] Resolve issue `01-unsigned-notarization`: provide signing/notarization
  credentials/process or explicitly accept the unsigned candidate and its
  documented limitation.
- [ ] Decide npm registry, package access, first-publication policy for core
  and dev, and provide a short-lived npm token with required scope. No npm
  publish command or secret exists in this repository.
- [ ] Decide whether any Rust crate is published separately; this preparation
  does not imply crates.io publication.
- **Recommendation only:** because Unreleased has matured and includes the
  breaking `PointerAction` removal, the next cut should be **0.3.0**, not
  **0.2.1**, under pre-1.0 SemVer; no version is selected.

**Current position:** **STOP (narrowed) — v0.2.0 is cut locally and tagged, but
publication remains blocked by the legal decision.** Push, npm publication,
signing, notarization, and all other distribution remain prohibited until legal
resolves issue 01. The GPL trio is eliminated from the distributed host graph;
the remaining STOP is the two missing license fields, `self_cell`'s dual-license
confirmation, weak-copyleft notices, and artifact-level legal review. No push,
registry publish, signing, or notarization was performed in this refresh.
User-owned `AGENTS.md` remains unstaged and untouched.

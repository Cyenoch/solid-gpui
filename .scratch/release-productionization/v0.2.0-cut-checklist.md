# v0.2.0 cut checklist

Status: **0.2.0 cut executed locally**; publication remains pending. No version,
tag, or changelog migration remains pending. This refresh records the post-cut
cycle at current HEAD.

Evidence release commit: `72a520c` (`chore(release): 0.2.0`). Annotated local tag:
`v0.2.0` (tag object `018f7f85e1c8aa756df14facdd7d7ff761fba683`).

## Cut execution record

- **Executed:** `2026-08-30`
- **Release commit:** `72a520c` (`chore(release): 0.2.0`)
- **Annotated local tag:** `v0.2.0`, pointing to `72a520c`; no push or other remote operation.
- **Changelog:** every former Unreleased entry moved under `## [0.2.0] - 2026-08-30`; the current cycle now has **7 Added capability / 6 Testing / 5 Fixed / 3 Changed** entries at refresh HEAD `dd32a70`.
- **Version/lock scope:** workspace Cargo, the three workspace package lines in `Cargo.lock`, and both package manifests synchronized to `0.2.0`; both Bun lockfiles were unchanged by the cut.
- **Publication boundaries:** no push, registry publish, signing, or notarization was performed. User-owned `AGENTS.md` remains unstaged and untouched.

## Fresh readiness refresh #15

- **Timestamp:** `2026-08-31T08:46:53+0800`.
- **Current HEAD:** `dd32a70` (`build(release): third-party notices bundle in host archive`).
- **Refresh delta from `bb31de6`:** 5 commits: `54c5993`, `ddd0e68`, `f90681c`, `093b32a`, and `dd32a70`.
- **Current Unreleased:** **7 Added capability / 6 Testing / 5 Fixed / 3
  Changed**; release-facing count is **13 additions / 5 fixes / 3 changes**.
- **Section accounting:** `dd32a70` adds the seventh `Added` bullet for the
  generated `THIRD-PARTY-NOTICES.md` host-archive inventory. The other four
  commits are documentation/evidence only: upstream issue drafts, the decision
  queue, evidence-index drift corrections, and the terms-determinability
  recheck. Counts are exact section counts, not inferred classifications.
- **License decision status:** **STOP narrowed, still ready-for-human.** GPL is
  eliminated from the distributed host graph by executed Option 3′ (stub).
  Terms for `gpui_shared_string` and `gpui_util` are **identified as
  Apache-2.0 by each crate's explicit `LICENSE-APACHE` evidence**, while their
  Cargo license metadata remains missing. The notices bundle is now shipped
  beside `LICENSE` in the host archive. STOP now rests only on D2 legal
  confirmation plus remaining standard obligations: `self_cell`, weak-
  copyleft/file-scope notices, and exact artifact review. Publication remains
  blocked; this is a legal decision, not an engineering waiver.
- **Decision queue:** [`DECISION-QUEUE.md`](DECISION-QUEUE.md) is the user's
  one-pager and the prominent human-action reference. D2 is the critical path;
  D1's upstream drafts are ready to post.
- **Option matrix:** Options 1 and 2 remain evidence-dead; **Option 3′ is
  EXECUTED**, reversible, and its README documents restoring the upstream
  dependency; Option 4 remains legal-only and is not selected.

## Fresh five-gate matrix

All five commands exited 0 and ran serially/exclusively under `/usr/bin/time -p`
at candidate HEAD `dd32a70`:

| Command | Outcome | Real time | Evidence |
| --- | ---: | ---: | --- |
| `make ci` | 0 | **58.40 s** | Rust workspace green; Core Bun **132/132**, **453,263 assertions**; dev Bun **24/24**, **56 assertions**; formatting, checks, builds, typechecks, package smoke, both Bun audits (**12/59** packages, no vulnerabilities), and cargo-deny advisories passed. The audit's committed-notices staleness guard regenerated the inventory and `cmp` passed. |
| `make embedded-bun` | 0 | **1.73 s** | `embedded_examples` **2 passed**; embedded counter **1 passed**; locked checks passed. |
| `make host-candidate-smoke` | 0 | **11.65 s** | Identical archive SHA-256 **`9b1760cbea118a4c2053c9074b84bd03f0f1efac7c3767f7964d8584192464b1`** on both archives; archive includes `THIRD-PARTY-NOTICES.md` and `SHA256SUMS`; 312-byte snapshot; expected process timeouts **5.012 s / 5.007 s**; version/help passed. |
| `make host-embedded-candidate-smoke` | 0 | **5.02 s** | Release embedded binary; `--smoke-press` reported `sent=true, commits=2, status=None`; expected timeout **4.575 s**; version/help passed. |
| `make bun-pack-smoke` | 0 | **2.03 s** | Frozen installs, both 0.2.0 JS/type builds and tarballs, external consumer install (**59 packages**), and `package tarball consumer smoke passed`. |

Successful sequential five-gate wall-time sum: **78.83 s**. Candidate timeout
exits are expected successful rehearsal behavior. The archive SHA is the
process-candidate check's identical pair of SHA-256 values.

## Capability and license notes

- **Five-commit crosswalk:** `54c5993` adds ready-to-post upstream license
  issue drafts; `ddd0e68` adds the user decision queue; `f90681c` corrects the
  evidence index's drifted counts/rows; `093b32a` records that the two missing
  Cargo fields have determinable Apache-2.0 terms from per-crate files while
  retaining the STOP; and `dd32a70` ships the generated notices inventory in
  the host archive and wires its staleness guard into `make audit`.
- **License status:** GPL is eliminated from the distributed graph by the
  executed stub. The remaining crates' terms are identified as Apache-2.0 by
  explicit per-crate `LICENSE-APACHE` files, but D2 must confirm that marking
  governs exact versions/artifacts and resolve `self_cell`, weak-copyleft
  notices, and standard artifact obligations.
- **Artifact evidence:** `THIRD-PARTY-NOTICES.md` inventories **659 Rust
  third-party packages / 1,152 package-license records** and **76 Bun
  dependency records**, and the host archive includes it next to `LICENSE`.
- **Human publication boundary:** D2 in [`DECISION-QUEUE.md`](DECISION-QUEUE.md)
  remains the only critical-path decision; D1's upstream drafts are ready to
  post. No publication, push, signing, or notarization was performed.

## Crosswalk from `bb31de6`

| Commit | Reachable evidence |
| --- | --- |
| `54c5993` | Ready-to-post upstream issue drafts in `.scratch/license-audit/upstream/01-license-fields.md` and `02-apache-gpui-gpl-ztracing.md`. |
| `ddd0e68` | User one-pager decision queue in `.scratch/release-productionization/DECISION-QUEUE.md`, with D2 first and D1 ready to post. |
| `f90681c` | Evidence-index drift corrections: tracked-file totals/area rows and corrected license/release evidence summaries. |
| `093b32a` | Terms-determinability recheck in `.scratch/license-audit/recheck-2026-08-31.md`, issue-01 wording, and synchronized upstream-draft/queue facts. |
| `dd32a70` | Generated `THIRD-PARTY-NOTICES.md`, archive inclusion/checksum wiring, candidate notices assertion, `make audit` staleness guard, and the Added changelog bullet. |

## Human decisions and publication boundaries
> **STOP (narrowed) — LICENSE BLOCKER:** Do not push, publish, sign, notarize, or otherwise distribute this cut until D2 obtains written legal/artifact confirmation for the exact distributed versions and artifacts. GPL is eliminated from the distributed host graph by Option 3′, and `gpui_shared_string`/`gpui_util` terms are identified as Apache-2.0 by explicit per-crate `LICENSE-APACHE` evidence. The remaining standard obligations are `self_cell`'s permitted option, weak-copyleft/file-scope notices, and exact artifact-level review. Publication is a legal decision, not an engineering waiver.

- [x] Existing local `0.2.0` cut/tag remains approved and unchanged.
- [x] Option 3′ local no-op stub landed; the matrix status is **EXECUTED**
  and the change is reversible through the README procedure.
- [x] `THIRD-PARTY-NOTICES.md` is generated, committed, included next to
  `LICENSE` in the host archive, and protected by the `make audit` staleness
  guard.
- [ ] **D2 — Confirm the remaining license items with legal:** written
  confirmation that the Apache-2.0 marking governs exact crate versions and
  artifacts; `self_cell`'s permitted option; weak-copyleft/file-scope notices;
  and exact artifact-level re-audit.
- [ ] **D1 — Post upstream drafts:** issue 01 is ready to post; issue 02 is
  optional and must be ordered deliberately because it discusses private
  downstream context.
- [ ] Resolve issue `01-unsigned-notarization`: provide signing/notarization
  credentials/process or explicitly accept the unsigned candidate and its
  documented limitation.
- [ ] Decide npm registry, package access, first-publication policy for core
  and dev, and provide a short-lived npm token with required scope. No npm
  publish command or secret exists in this repository.
- [ ] Decide whether any Rust crate is published separately; this preparation
  does not imply crates.io publication.
- **Recommendation only:** because Unreleased has matured and includes the
  breaking `PointerAction` removal, the next cut should be **0.3.0**, not
  **0.2.1**, under pre-1.0 SemVer; no version is selected.

**Current position:** **STOP (narrowed) — v0.2.0 is cut locally and tagged, but
publication remains blocked by D2's legal/artifact confirmation.** Push, npm
publication, signing, notarization, and all other distribution remain
prohibited until D2 resolves the exact Apache-marked crate treatment,
`self_cell`, weak-copyleft notices, and artifact review. The notices inventory
is shipped and guarded for staleness, but it is an inventory rather than legal
clearance. No push, registry publish, signing, or notarization was performed in
this refresh. User-owned `AGENTS.md` remains unstaged and untouched.

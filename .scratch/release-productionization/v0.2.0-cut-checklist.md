# v0.2.0 cut checklist

Status: **0.2.0 cut executed locally**; publication remains pending. No version,
tag, or changelog migration remains pending. This refresh records the post-cut
cycle at current HEAD.

Evidence release commit: `72a520c` (`chore(release): 0.2.0`). Annotated local tag:
`v0.2.0` (tag object `018f7f85e1c8aa756df14facdd7d7ff761fba683`).

## Cut execution record

- **Executed:** `2026-08-30`
- **Release commit:** `72a520c` (`chore(release): 0.2.0`)
- **Annotated local tag:** `v0.2.0`, pointing to `72a520c`; no push or other remote operation.
- **Changelog:** every former Unreleased entry moved under `## [0.2.0] - 2026-08-30`; the current cycle now has **6 Added capability / 5 Testing / 3 Fixed / 2 Changed** entries at final HEAD `d74467e`.
- **Version/lock scope:** workspace Cargo, the three workspace package lines in `Cargo.lock`, and both package manifests synchronized to `0.2.0`; both Bun lockfiles were unchanged by the cut.
- **Publication boundaries:** no push, registry publish, signing, or notarization was performed. User-owned `AGENTS.md` remains unstaged and untouched.

## Fresh readiness refresh #13

- **Timestamp:** `2026-08-31T05:19:52+0800`.
- **Current HEAD:** `dedf7c9` (`docs: option 3 fork proof spike`).
- **Refresh delta from `948282e`:** 5 commits: `c329b63`, `eb9dcb9`,
  `02a640e`, `8e590fd`, and `dedf7c9`.
- **Current Unreleased:** **6 Added capability / 5 Testing / 5 Fixed / 2
  Changed**; release-facing count is **11 additions / 5 fixes / 2 changes**.
- **Section accounting:** the five commits add event-storm evidence, the
  license audit and feasibility matrix, synchronized index rows, and the
  Option 3 fork proof; none adds a CHANGELOG bullet. Counts are exact section
  counts, not inferred classifications.
- **License decision status:** **publication STOP.** Option 1 (feature off)
  and Option 2 (pin bump to observed `origin/main`) are evidence-dead. Option 3
  is proven narrowly and conditionally by `dedf7c9` for the macOS target
  no-dev graph, with medium/high ongoing fork maintenance cost. Option 4 is
  legal-only; it preserves the upstream graph and requires written legal
  acceptance plus artifact obligations. The two missing-license crates remain
  unresolved under every option.

## Fresh five-gate matrix

All five commands exited 0 and ran serially/exclusively under `/usr/bin/time -p`
at candidate HEAD `dedf7c9`:

| Command | Outcome | Real time | Evidence |
| --- | ---: | ---: | --- |
| `make ci` | 0 | **56.23 s** | Core Bun **132/132**, **453,263 assertions**; dev Bun **24/24**, **56 assertions**; Rust, builds, typechecks, package smoke, both Bun audits (12/59 packages), and cargo-deny (`advisories ok`) passed. |
| `make embedded-bun` | 0 | **4.26 s** | `embedded_examples` **2 passed**; embedded counter **1 passed**; locked checks passed. |
| `make host-candidate-smoke` | 0 | **11.92 s** | Identical archive SHA-256 **`a6086a7f62be4b1604a88925fb186e1df5b6c90a2783f5c67729b4bba7b3076e`**; 312-byte snapshot; expected process timeouts **5.021 s/5.021 s**; version/help passed. |
| `make host-embedded-candidate-smoke` | 0 | **5.08 s** | `--smoke-press`: `sent=true, commits=2, status=None`; expected timeout **4.569 s**; version/help passed. |
| `make bun-pack-smoke` | 0 | **2.03 s** | Frozen installs, both 0.2.0 JS/type builds and tarballs, external consumer install (**59 packages**), and package tarball consumer smoke passed. |

Successful sequential five-gate wall-time sum: **79.52 s**. Candidate timeout
exits are expected successful rehearsal behavior.

## Capability notes

- **Event-storm sample (`c329b63`):** the 23.6-second sample remains the
  minimum stable sample; shorter samples fail the variance/margin gate.
- **License audit (`eb9dcb9`):** the default host graph reaches the GPL-3.0-or-
  later `zlog`, `ztracing`, and `ztracing_macro` crates, plus
  `gpui_shared_string` and `gpui_util` without manifest license fields. The
  audit is a legal blocker, not an engineering waiver.
- **Feasibility (`02a640e`):** no feature-off switch exists and the observed
  upstream pin bump clears none of the five findings. A two-crate fork is the
  only actionable technical removal path currently evidenced.
- **Option 3 spike (`dedf7c9`):** all three GPL package IDs disappeared from
  the target-qualified no-dev graph; host check, release build, and version
  smoke passed. Standalone translation and ongoing maintenance are medium/high
  cost; the two unknown-license crates remain.

## Crosswalk from `948282e`

| Commit | Reachable evidence |
| --- | --- |
| `c329b63` | Event-storm sample-budget verdict in `.scratch/storm-trim/notes.md`. |
| `eb9dcb9` | License audit and publication STOP in `.scratch/license-audit/2026-08-30.md` and issue 01. |
| `02a640e` | Four-option GPL linkage feasibility matrix in `.scratch/license-audit/feasibility.md`. |
| `8e590fd` | Synchronized `.scratch/EVIDENCE.md` index. |
| `dedf7c9` | Narrow Option 3 fork proof in `.scratch/license-audit/spike-option3.md`, plus issue pointer. |

## Human decisions and publication boundaries
> **STOP — LICENSE BLOCKER:** Do not push, publish, sign, notarize, or otherwise distribute this cut until the release owner and a legal reviewer resolve [license audit issue 01](../license-audit/issues/01-gpl-and-unknown-host-licenses.md). The default `react-gpui-host` graph reaches GPL-3.0-or-later Zed crates, and two Zed crates have no manifest license field. Option 3 is only a conditional target no-dev graph proof; Option 4 is legal-only. Publication is a legal decision, not an engineering waiver.

- [x] Existing local `0.2.0` cut/tag remains approved and unchanged.
- [ ] Resolve the license blocker: written legal determination for GPL terms,
  both missing license fields, `self_cell`'s Apache option, weak-copyleft
  conditions, notices, source/corresponding-source or relinkable-object duties,
  and exact artifact-level re-audit.
- [ ] Resolve issue `01-unsigned-notarization`: provide signing/notarization
  credentials/process or explicitly accept the unsigned candidate and its
  documented limitation.
- [ ] Decide npm registry, package access, first-publication policy for core and
  dev, and provide a short-lived npm token with required scope. No npm publish
  command or secret exists in this repository.
- [ ] Decide whether any Rust crate is published separately; this preparation
  does not imply crates.io publication.
- **Recommendation only:** because Unreleased has matured and includes the
  breaking `PointerAction` removal, the next cut should be **0.3.0**, not
  **0.2.1**, under pre-1.0 SemVer; no version is selected.

**Current position:** **STOP — v0.2.0 is cut locally and tagged, but publication
is blocked by the license decision.** Push, npm publication, signing,
notarization, and all other distribution remain prohibited until legal resolves
issue 01. No push, registry publish, signing, or notarization was performed in
this refresh. User-owned `AGENTS.md` remains unstaged and untouched.

# v0.2.0 cut checklist

Status: **0.2.0 cut executed locally**; publication remains pending. No version,
tag, or changelog migration remains pending. This refresh records the new cycle
after the local cut.

Evidence release commit: `72a520c` (`chore(release): 0.2.0`). Annotated local tag:
`v0.2.0` (tag object `018f7f85e1c8aa756df14facdd7d7ff761fba683`).

## Cut execution record

- **Executed:** `2026-08-30`
- **Release commit:** `72a520c` (`chore(release): 0.2.0`)
- **Annotated local tag:** `v0.2.0`, pointing to `72a520c`; no push or other remote operation.
- **Changelog:** every former Unreleased entry moved under `## [0.2.0] - 2026-08-30`; the new cycle now has **3 Added capability / 4 Testing / 3 Fixed / 1 Changed** entries, exact at final HEAD `a32b53d`.
- **Version/lock scope:** workspace Cargo, the three workspace package lines in `Cargo.lock`, and both package manifests synchronized to `0.2.0`; both Bun lockfiles were unchanged.
- **Publication boundaries:** no push, registry publish, signing, or notarization was performed. User-owned `AGENTS.md` remained unstaged and untouched.

## Fresh readiness refresh #9

- **Timestamp:** `2026-08-30T10:17:41+0800`
- **Current HEAD:** `a32b53d` (`refactor(api): prune dead alias and document audit follow-ups`).
- **Refresh delta from `794183d`:** 5 commits: `be5fe29`, `486fb71`, `60b3ddf`, `f0e725b`, and `a32b53d`.
- **Current Unreleased:** **3 Added capability / 4 Testing / 3 Fixed / 1 Changed**; release-facing count is **7 additions / 3 fixes / 1 change** when Testing is counted with additions.
- **Capabilities:** surface lifecycle property invariants, renderer soak plateau, TypeScript commit-emission property invariants, public export audit and follow-ups, verified standalone distribution pair, onboarding dogfood verdict, actionable diagnostics, contributor guide, documentation index, and shared multi-surface example.
- **Property and soak evidence:** surface 16 seeds x 48 operations; tree 32 seeds x 64 operations; TypeScript 24 seeds x 40 mutations; renderer soak 2,000 iterations with flat post-warm-up RSS/side maps. All focused evidence is covered by `make ci`.
- **Fuzz:** deterministic protocol coverage remains **35/35 command seeds and 23/23 event seeds** in both Rust and TypeScript records.
- **GPUI drift:** verdict remains **no released unblocks** for recorded upstream boundaries.

## Fresh five-gate matrix

All five commands exited 0 and ran serially/exclusively under `/usr/bin/time -p`
at candidate HEAD `a32b53d`, in the order shown below. Candidate timeout exits are
expected successful rehearsal behavior.

| Command | Outcome | Real time | Evidence |
| --- | ---: | ---: | --- |
| `make ci` | 0 | **76.89 s** | Rust workspace **216 passed / 0 failed**; Core Bun **132 pass / 0 fail / 453,263 expect() calls**; dev Bun **24 pass / 0 fail / 56 expect() calls**; formatting, checks, builds, typechecks, package smoke, property, soak, and performance guards passed. |
| `make embedded-bun` | 0 | **1.84 s** | `embedded_examples` **2 passed**; embedded counter **1 passed**; locked checks passed. |
| `make host-candidate-smoke` | 0 | **35.48 s** | Identical archive SHA-256 **`6c4a83aea2b6fcdfe8d28ac464f7d681bbff341eee1c6f38cdfbd081425c8b87`**; archive `react-gpui-host-0.2.0-aarch64-apple-darwin.tar.gz`; snapshot commit **312 bytes**; expected process timeouts **5.009 s/5.015 s**; version/help passed. |
| `make host-embedded-candidate-smoke` | 0 | **20.56 s** | Release embedded binary; `--smoke-press` reported `sent=true, commits=2, status=None`; expected timeout **4.534 s**; version/help passed. |
| `make bun-pack-smoke` | 0 | **2.10 s** | Frozen installs, both 0.2.0 JS/type builds and tarballs, external consumer install (**59 packages**), and `package tarball consumer smoke passed`. |

Successful sequential five-gate wall-time sum: **136.87 s**. Candidate timeout
lines are expected behavior, not gate failures. The archive SHA is the
process-candidate check's identical pair of SHA-256 values.

## Capability notes

- Surface property invariants (`be5fe29`) cover fresh/reused and retired IDs,
  closure bookkeeping, cross-surface focus/activation, side-map ownership, and
  surface/epoch mismatch rejection across 16 seeds x 48 operations.
- Renderer soak (`486fb71`) covers 2,000 deterministic patch/draw iterations
  on a fixed 500-node rich-text/input/VirtualList tree. Side maps remained
  constant; RSS rose 608 KiB only through allocator/GPUI warm-up, a
  **plateau/flat** verdict far below the 50 MiB finding threshold.
- TypeScript emission property (`60b3ddf`) covers 24 seeds x 40 mutations,
  960 post-bootstrap commits plus 24 snapshots, with frame round trips and
  node/listener/revision invariants; targeted evidence is 362,716 assertions
  in 1.75 s.
- Export audit baseline/follow-ups (`f0e725b`, `a32b53d`): baseline core 116 /
  dev 20; Tier 1 core 41/10/65 and dev 9/11/0; Tier 2 core 111/2/3 and dev
  16/4/0. Cleanup removed dead `PointerAction`, leaving core 115/dev 20, and
  documented `createStyleSheet`, `Position`, transport details, root options,
  notification responses, and transition payloads. `AnimationCompleteEvent`
  and `TransportTerminationDetails` remain future-surface follow-ups.

## Mechanical cut sequence (executed)

1. [x] Moved the former Unreleased content under `## [0.2.0] - 2026-08-30` and left an empty `## [Unreleased]` section.
2. [x] Ran `make release-prep VERSION=0.2.0`; reviewed manifest and lockfile diffs.
3. [x] Ran the five release gates serially and exclusively before the release commit/tag.
4. [x] Reviewed the dated changelog, generated locks, archive name, and `git diff --check`; committed the version cut as `72a520c`.
5. [x] Created the local annotated tag `v0.2.0` after all five pre-cut gate outcomes passed. No push or publication was performed.

## Human decisions and publication boundaries

- [x] Approve release version `0.2.0` and release date `2026-08-30` for the local cut.
- [ ] Resolve issue `01-unsigned-notarization`: provide signing/notarization credentials/process or explicitly accept the unsigned candidate and its documented limitation.
- [x] Approve tag convention `v0.2.0`; the annotated tag was created locally only after all five pre-cut gate outcomes passed.
- [ ] Decide npm registry, package access, first-publication policy for core and dev, and provide a short-lived npm token with required scope. No npm publish command or secret exists in this repository.
- [ ] Decide whether any Rust crate is published separately; this preparation does not imply crates.io publication.

**Current position:** v0.2.0 cut locally + tagged, publication (push/npm/signing)
pending human action. No push, registry publish, signing, or notarization was
performed in this refresh. User-owned `AGENTS.md` remains unstaged and untouched.

This file is local evidence under `/.scratch/`, ignored by `.gitignore` by
default. The readiness report and checklist follow the repository's force-add
evidence convention; neither is a published artifact. No publication option is
selected by this checklist.

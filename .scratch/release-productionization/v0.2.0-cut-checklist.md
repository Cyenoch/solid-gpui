# v0.2.0 cut checklist

Status: mechanical preparation only; no version, tag, publish, or changelog
migration has been performed.
Evidence HEAD: `06922c3` (`feat(examples): adopt appearance-aware theming`).

## Evidence from this preparation pass

- Current versions are synchronized at `0.1.0`: workspace Cargo, `react-gpui`
  and `react-gpui-host`/`react-gpui-bun` metadata, `@react-gpui/core`, and
  `@react-gpui/dev`.
- The current Unreleased inventory is **120 Added/Fixed/Changed entries**:
  **104 Added**, **9 Fixed**, and **7 Changed**.
- `git log --oneline 5ca34e1..HEAD` reports **51 commits** in this refresh
  delta, including selectable Text, surface options, gallery/layout repairs,
  focus/overlay and drag input, transport/protocol hardening, cross-platform
  runner scaffolding, TestApp/examples, event-storm budgets, animation,
  clipboard/word navigation, VirtualList restoration, and appearance-aware
  example theming.
- The protocol terminal is **22 commands / 22 events**. Current required wire
  forms are TextInput 13-slot, Image 4-slot, Drag 5-slot, CommandResult
  7-slot, string Submit, and three-number WindowResize; OpenSurface retains
  its valid no-options form plus an optional creation-options tuple. API locks
  are **core 110 / dev 20** exports.
- VirtualList uses GPUI's native variable-height `list` with per-node
  `ListState`, real measured committed rows, estimated placeholders outside the
  committed range, `ListState`-based scroll commands, and logical item/offset
  restoration across item-count changes with end clamping.
- Image `fallbackSource` uses the current tag-3 four-slot host tuple and GPUI
  `with_loading`/`with_fallback` visual states; JavaScript `onError` remains an
  upstream gap. Window resize uses its required three-number payload,
  three-argument callbacks, expanded `WindowSize` stores, and scale-only
  observer updates.
- Outbound file drag uses host-properties tag 4
  `[4,dragType|null,exportFiles|null,acceptsDragOver,acceptsDrop]` with 1..8
  validated host-local paths; keybinding registration is root-only command 22
  with full replacement and Event 17 routing.
- TextInput single- and multiline geometry use precise cached shaped/wrapped
  layouts with UTF-16 line mapping. Host-owned clipboard editing, select-all,
  word navigation, UAX #29 double-click, and triple-click line selection are
  implemented; multiline IME candidate placement remains display-backed.
- Selectable read-only Text is implemented as a host-owned visual surface with
  an optional Text-only wire tail, native-shaped highlights, and host-side
  Cmd/Ctrl-C copy, without a JavaScript selection event or mirrored state.
- `pointerEvents` remains intentionally unexposed because normal native
  hitboxes provide basic pass-through without a listener; `Root.zoom()` has
  headless command coverage but its visual toggle remains display-backed.
- Appearance-aware theme tokens now drive the examples; this is an examples/
  docs change with no library API change. The focused examples and
  `@react-gpui/dev` `renderTestApp` facade remain available for consumer
  behavior-level testing.
- The cross-platform support matrix is scaffolded at
  `.github/workflows/cross-platform.yml` for locked Linux/Windows checks, but
  runner validation and display-backed smoke remain unvalidated until push/PR
  execution. `.scratch/release-productionization/upstream-dependencies.md`
  records the true upstream gaps and test-platform boundaries.
- `.scratch/release-productionization/dependency-audit.md` records the current
  lockfile/toolchain inventory and explicitly states that cargo-audit is absent.
- Changelog migration dry-run preview (date is a human decision):

  ```diff
  @@ CHANGELOG.md header @@
  -## [Unreleased]
  +## [0.2.0] - <release-date>
  ```

- `scripts/release-prep.sh 0.2.0` accepts the strict SemVer argument but safely
  stops before mutation because `[0.2.0]` does not exist yet. It currently
  requires the exact heading `## [0.2.0]`; its validation does **not** accept the
  Keep a Changelog date suffix. Either broaden that guard before the cut or
  temporarily create the exact heading, run prep, and add the date afterward.
- Once its changelog guard passes, `make release-prep VERSION=0.2.0` updates the
  workspace Cargo version and both package JSON versions, refreshes Cargo/Bun
  locks, runs Cargo check, and runs both normal and frozen Bun installs. The
  host and Bun crates inherit the workspace version; no host manifest version
  edit is needed. The script does not edit CHANGELOG, create tags, publish, or
  authenticate registries.
- The current five-gate matrix remains green on final HEAD `06922c3`: latest
  `make ci` (**59.25 s**) includes Rust **169** and Bun core108/dev18
  (**53,212/46 assertions**); `make embedded-bun` (**2.71 s**),
  `make host-candidate-smoke` (**13.05 s**),
  `make host-embedded-candidate-smoke` (**5.68 s**), and
  `make bun-pack-smoke` (**2.63 s**). The serial protocol golden generator
  took **0.97 s**; API surface generation took **2.13 s** then **2.06 s**,
  with zero fixture drift on both passes. The sequential five-gate sum is
  **83.32 s**.
- `make host-release-check`/candidate smoke use the host version from
  `cargo metadata` and the target from `rustc -vV`. The current archive is
  `dist/react-gpui-host-0.1.0-aarch64-apple-darwin.tar.gz`; after synchronization,
  the archive name and `--version` check automatically use `0.2.0`. The archive
  remains unsigned; SHA-256 files prove archive consistency, not publisher identity.

## Human decisions before cutting

- [ ] Approve release version `0.2.0` and the release date.
- [ ] Resolve issue `01-unsigned-notarization`: either provide signing and
      notarization credentials/process, or explicitly accept the unsigned
      candidate and its documented limitation.
- [ ] Choose and approve tag convention `v0.2.0` (recommended); create it only
      after all gates and the changelog migration pass.
- [ ] Decide npm registry, package access, first-publication policy for core and
      dev, and provide a short-lived npm token with the required scope. No npm
      publish command or secret exists in this repository.
- [ ] Decide whether any Rust crate is published separately; this preparation
      does not imply crates.io publication.

## Mechanical cut sequence after approval

1. Update the release-prep heading guard to accept the dated heading, if the
   dated Keep a Changelog form is retained. Move the current Unreleased content
   under `## [0.2.0] - YYYY-MM-DD` and leave an empty `## [Unreleased]` section.
2. Run `make release-prep VERSION=0.2.0`; review all manifest and lockfile diffs.
3. Run `make ci` and `make embedded-bun`.
4. Run `make host-release-check`, then `make host-release-bundle`; inspect the
   archive allowlist, checksums, target, version output, and unsigned status.
5. Run both package dry-runs (`bun pm pack --dry-run` in each package) and the
   package consumer smoke already covered by `make ci`.
6. Review the dated changelog, generated locks, archive name, and `git diff
   --check`; commit the version cut and release notes.
7. After the human approves the final commit, create the `v0.2.0` tag and push
   only through the separately approved release procedure. Publish npm packages
   only after registry/token/access decisions are confirmed.

The implementation backlog for the current 0.2.0 contract is empty. Remaining
items are documented platform acceptance boundaries or human release decisions:
unsigned/notarization, the future-incompatibility choice, display-backed
validation, cross-platform runner evidence, upstream TextInput/RTL/style/Image
gaps, and npm/crates.io publication policy. The cross-platform workflow is
scaffolded but unvalidated until push/PR runners execute; local macOS probes are
not runner proof. A prior full-CI attempt timed out in the unrelated
`tests::transport::process_event_writer_kills_closed_stdin_child_for_reader_eof`
test; two immediate isolated reruns passed and no transport files changed. The
human release owner controls the version/date decision, artifact signing, tag
creation, registry credentials, and publication.

This file is local evidence under `/.scratch/`, which is ignored by
`.gitignore` by default. The readiness report and this checklist are
force-added under the repository's evidence convention; neither is a published
artifact. The companion readiness report is the committed-tree-facing summary;
its five-gate matrix and this checklist's seven-step sequence are consistent.

- The readiness report and this checklist both reflect final HEAD `06922c3`, the
  120-entry Unreleased inventory (104/9/7), protocol 22/22 terminal, 42-slot
  style, precise single/multiline TextInput geometry, selectable Text,
  clipboard/select-all/word navigation, Image fallback, scale-factor
  observation, keybinding 22, outbound drag tag 4, focus/overlay and
  appearance-aware example updates, single-form wire cutover, and the
  three-tier upstream tracking note.

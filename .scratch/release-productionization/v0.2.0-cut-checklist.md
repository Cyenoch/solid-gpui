# v0.2.0 cut checklist

Status: **0.2.0 cut executed locally**; publication remains pending. No version,
tag, or changelog migration remains pending. This refresh records the post-cut
cycle at current HEAD.

Evidence release commit: `72a520c` (`chore(release): 0.2.0`). Annotated local tag:
`v0.2.0` (tag object `018f7f85e1c8aa756df14facdd7d7ff761fba683`).

## Cut execution record

- **Executed:** `2026-08-30`
- **Release commit:** `72a520c` (`chore(release): 0.2.0`)
- **Annotated local tag:** `v0.2.0`, pointing to `72a520c`; no push or other remote operation.
- **Changelog:** every former Unreleased entry moved under `## [0.2.0] - 2026-08-30`; the current cycle now has **6 Added capability / 5 Testing / 3 Fixed / 2 Changed** entries at final HEAD `d74467e`.
- **Version/lock scope:** workspace Cargo, the three workspace package lines in `Cargo.lock`, and both package manifests synchronized to `0.2.0`; both Bun lockfiles were unchanged by the cut.
- **Publication boundaries:** no push, registry publish, signing, or notarization was performed. User-owned `AGENTS.md` remains unstaged and untouched.

## Fresh readiness refresh #12

- **Timestamp:** `2026-08-31T01:01:19+0800`.
- **Current HEAD:** `2b3f39a` (`chore(meta): publish readiness`).
- **Refresh delta from `d74467e`:** 4 commits: `a1e7683`, `064e4b0`, `003060b`, and `2b3f39a`.
- **Current Unreleased:** **6 Added capability / 5 Testing / 5 Fixed / 2 Changed**; release-facing count is **11 additions / 5 fixes / 2 changes**.
- **Section accounting:** `a1e7683` adds the rendererVersion Fixed entry and `064e4b0` adds the root-test Fixed entry. The rehearsal and publication metadata commits add evidence but no Unreleased bullets. Counts are exact section counts, not inferred classifications.
- **Capabilities:** four-source release synchronization, root-test scoping, 0.3.0 rehearsal with hydration-difference verdict, publication metadata readiness (npm allowlists, crates SPDX, unsafe FFI inventory), and layered supply-chain status.
- **Supply-chain boundary:** Bun audits are clean; cargo-deny reports `advisories ok` with `paste`, `rustybuzz`, and transitive `ttf-parser` documented; `skrifa` removes the application-owned direct parser edge.

## Fresh five-gate matrix

All five commands exited 0 and ran serially/exclusively under `/usr/bin/time -p`
at candidate HEAD `2b3f39a`. The initial CI attempt exited 2 after **2,416.86 s**
on the event-storm timeout. No orphan cargo/Bun/test processes were found;
the isolated event-storm rerun passed in **23.61 s**, and warm CI passed in
**64.36 s**. This is an environment-load flake, not a source regression.

| Command | Outcome | Real time | Evidence |
| --- | ---: | ---: | --- |
| `make ci` (warm rerun) | 0 | **64.36 s** | Core Bun **132/132**, **453,263 assertions**; dev Bun **24/24**, **56 assertions**; Rust, builds, typechecks, package smoke, both Bun audits (12/59 packages), and cargo-deny (`advisories ok`) passed. Initial run: exit 2 / 2,416.86 s due to event-storm timeout; isolated rerun: 1/1 / 23.61 s. |
| `make embedded-bun` | 0 | **7.11 s** | `embedded_examples` **2 passed**; embedded counter **1 passed**; locked checks passed. |
| `make host-candidate-smoke` | 0 | **12.50 s** | Identical archive SHA-256 **`a6086a7f62be4b1604a88925fb186e1df5b6c90a2783f5c67729b4bba7b3076e`**; 312-byte snapshot; expected process timeouts **5.006 s/5.014 s**; version/help passed. |
| `make host-embedded-candidate-smoke` | 0 | **5.22 s** | `--smoke-press`: `sent=true, commits=2, status=None`; expected timeout **4.647 s**; version/help passed. |
| `make bun-pack-smoke` | 0 | **2.11 s** | Frozen installs, both 0.2.0 JS/type builds and tarballs, external consumer install (**59 packages**), and package tarball consumer smoke passed. |

Successful sequential five-gate wall-time sum: **91.20 s**.

## Capability notes

- **Four-source release sync (`a1e7683`):** `release-prep` now keeps Cargo workspace, both Bun package manifests, and `rendererVersion` synchronized; the version-const note records that React DevTools consumes this package metadata.
- **Root-test scoping (`064e4b0`):** root Bun discovery ignores generated `target/**`; no separate root-buntest notes file exists, while CONTRIBUTING documents `make bun-test`.
- **0.3.0 rehearsal (`003060b`):** all four sources moved 0.2.0 → 0.3.0 in the detached worktree; the first CI attempt needed a frozen local dependency hydration refresh, then passed. Archive reproducibility passed with SHA `7a39f7067025afa14f33f6874e54e9defc6ccf56fe33a83c1fe493a4cd51e944`; worktree hydration difference is known and explicit.
- **Publish metadata (`2b3f39a`):** npm package allowlists are exact (core 18 / dev 8 files); Cargo metadata uses SPDX `Apache-2.0`; unsafe embedded Bun/JSC FFI locations and adjacent safety comments are inventoried. No publish command ran.

## Crosswalk from `d74467e`

| Commit | Reachable evidence |
| --- | --- |
| `a1e7683` | Truthful renderer version constant and four-source release-prep wiring; `.scratch/version-const/notes.md`. |
| `064e4b0` | Root Bun test discovery scoped away from generated `target/**`; CONTRIBUTING and testing-dx notes document the entry. |
| `003060b` | Detached 0.3.0 rehearsal, four-source sync, deterministic archive, and known hydration difference in `.scratch/rehearsal-2/notes.md`. |
| `2b3f39a` | npm/crates publication metadata and unsafe FFI audit in `.scratch/pub-meta/2026-08-30.md`. |

## Human decisions and publication boundaries
> **STOP — LICENSE BLOCKER:** Do not push, publish, sign, notarize, or otherwise distribute this cut until the release owner and a legal reviewer resolve [license audit issue 01](../license-audit/issues/01-gpl-and-unknown-host-licenses.md). The default `react-gpui-host` graph reaches GPL-3.0-or-later Zed crates, and two Zed crates have no manifest license field; `cargo deny check licenses` is intentionally still red. Publication is a legal decision, not an engineering waiver. Use the evidence in [the audit notes](../license-audit/2026-08-30.md) to confirm the license choice, notices, and source obligations for every artifact.


- [x] Existing local `0.2.0` cut/tag remains approved and unchanged.
- [ ] Resolve issue `01-unsigned-notarization`: provide signing/notarization credentials/process or explicitly accept the unsigned candidate and its documented limitation.
- [ ] Decide npm registry, package access, first-publication policy for core and dev, and provide a short-lived npm token with required scope. No npm publish command or secret exists in this repository.
- [ ] Decide whether any Rust crate is published separately; this preparation does not imply crates.io publication.
- **Recommendation only:** because Unreleased has matured and includes the breaking `PointerAction` removal, the next cut should be **0.3.0**, not **0.2.1**, under pre-1.0 SemVer; no version is selected.

**Current position:** v0.2.0 cut locally + tagged, publication (push/npm/signing)
pending human action. No push, registry publish, signing, or notarization was
performed in this refresh. User-owned `AGENTS.md` remains unstaged and
untouched.

# v0.2.0 cut checklist

Status: mechanical preparation only; no version, tag, publish, or changelog
migration has been performed.
Evidence HEAD: `a097f3c` (`feat(protocol): carry pointer coordinates`).

## Evidence from this preparation pass

- Current versions are synchronized at `0.1.0`: workspace Cargo, `react-gpui`
  and `react-gpui-host`/`react-gpui-bun` metadata, `@react-gpui/core`, and
  `@react-gpui/dev`.
- The current Unreleased inventory is **128 Added/Fixed/Changed entries**:
  **109 Added**, **12 Fixed**, and **7 Changed**.
- `git log --oneline 5ab0794..HEAD` reports **15 commits** in this refresh
  delta, spanning typed surface errors, tooltips and close/context-menu policy,
  deterministic gate waits, accessibility semantics, native keyboard tab-stop
  traversal and focus restoration, text-file commands and notes persistence,
  interchange boundaries, clipboard images, the embedded representative
  example matrix, and pointer coordinates.
- The protocol terminal is **28 commands / 23 events**. Current required wire
  forms are TextInput 13-slot, Image 4-slot, Drag 5-slot, Pointer 7-slot,
  CommandResult 7-slot, string Submit, and three-number WindowResize;
  OpenSurface retains its valid no-options form plus an optional
  creation-options tuple. API locks are **core 114 / dev 20** exports.
- Pointer press/release payloads use strict
  `[6,button,modifiers,action,clickCount,x,y]` coordinates in finite,
  non-negative logical window pixels; legacy five-field payloads are rejected.
- VirtualList uses GPUI's native variable-height `list` with per-node
  `ListState`, real measured committed rows, estimated placeholders outside
  the committed range, `ListState`-based scroll commands, and logical
  item/offset restoration across item-count changes with end clamping.
- Image `fallbackSource` uses the current tag-3 four-slot host tuple and GPUI
  `with_loading`/`with_fallback` visual states; stable element IDs prevent
  state collisions across mounted images, SVG bytes load through the pinned
  GPUI image element, and JavaScript `onError` remains an upstream gap. Window
  resize uses its required three-number payload and scale-only observer updates.
- Root text-file commands 25/26 perform bounded asynchronous UTF-8 I/O, and
  root clipboard-image commands 27/28 carry bounded PNG, JPEG, GIF, or SVG
  bytes. Unsupported X11/Wayland image clipboard operations reject explicitly.
- Focus traversal reaches React View, Pressable, TextInput, and selectable Text
  nodes through native tab stops; focused unmount emits blur and restores focus.
- Embedded coverage includes serial gallery/text-input/virtual-list/notes
  startup snapshots and a Fast Refresh lifecycle probe.
- The cross-platform support matrix is scaffolded at
  `.github/workflows/cross-platform.yml` for locked Linux/Windows checks, but
  runner validation and display-backed smoke remain unvalidated until push/PR
  execution. `.scratch/release-productionization/upstream-dependencies.md`
  records the true upstream gaps and test-platform boundaries.
- `.scratch/release-productionization/dependency-audit.md` records the current
  lockfile/toolchain inventory and explicitly states that cargo-audit is absent.
- Changelog migration dry-run preview (date is a human decision):

  ```diff
  @@ CHANGELOG.md header @@
  -## [Unreleased]
  +## [0.2.0] - <release-date>
  ```

- `scripts/release-prep.sh 0.2.0` accepts the strict SemVer argument but safely
  stops before mutation because `[0.2.0]` does not exist yet. It currently
  requires the exact heading `## [0.2.0]`; its validation does not accept the
  Keep a Changelog date suffix. Either broaden that guard before the cut or
  temporarily create the exact heading, run prep, and add the date afterward.
- Once its changelog guard passes, `make release-prep VERSION=0.2.0` updates
  the workspace Cargo version and both package JSON versions, refreshes Cargo/Bun
  locks, runs Cargo check, and runs both normal and frozen Bun installs. The
  host and Bun crates inherit the workspace version; no host manifest version
  edit is needed. The script does not edit CHANGELOG, create tags, publish, or
  authenticate registries.
- The final five-gate matrix is green at final HEAD `a097f3c`. `make ci` ran
  immediately before the pointer-coordinate commit (**38.12 s**, exit 0; Core
  Bun **124 tests / 53,299 assertions**, dev Bun **18 tests / 46 assertions**).
  The four final-tree gates then passed: `make embedded-bun` (**3.20 s**, exit
  0; embedded examples **2 passed** and embedded counter **1 passed**),
  `make host-candidate-smoke` (**38.11 s**, exit 0),
  `make host-embedded-candidate-smoke` (**21.03 s**, exit 0), and
  `make bun-pack-smoke` (**2.39 s**, exit 0). Sequential wall-time sum:
  **102.85 s**. Candidate timeout lines were expected. Host archive SHA-256
  was identical across builds:
  `e7d496636e1404a8c719c5dcff02cf910e99dd263d84cb2ecb1ece3b9c03db1a`.
- `make embedded-bun` is **3.20 s** versus the former **2.21 s** baseline because
  it includes the serial four-entry representative examples startup matrix and
  Fast Refresh lifecycle probe; this is the intended embedded coverage-round
  cost.
- Focused Image tests and checked-in API/golden checks passed within the fresh
  `make ci`; pointer-coordinate vectors passed in the final-tree golden checks.
- The prior citation audit checked 90 guide/README references (20
  getting-started, 43 troubleshooting, 27 root README): 3 wrong, 3 fixed, 0
  remaining. `docs/protocol.md` had 290 citation occurrences and 480 numeric
  range segments with 0 out of range in that audit. Those figures are prior
  evidence, not a new citation audit in this refresh.

## Human decisions before cutting

- [ ] Approve release version `0.2.0` and the release date.
- [ ] Resolve issue `01-unsigned-notarization`: either provide signing and
      notarization credentials/process, or explicitly accept the unsigned
      candidate and its documented limitation.
- [ ] Choose and approve tag convention `v0.2.0` (recommended); create it only
      after all gates and the changelog migration pass.
- [ ] Decide npm registry, package access, first-publication policy for core and
      dev, and provide a short-lived npm token with the required scope. No npm
      publish command or secret exists in this repository.
- [ ] Decide whether any Rust crate is published separately; this preparation
      does not imply crates.io publication.

## Mechanical cut sequence after approval

1. Update the release-prep heading guard to accept the dated heading, if the
   dated Keep a Changelog form is retained. Move the current Unreleased content
   under `## [0.2.0] - YYYY-MM-DD` and leave an empty `## [Unreleased]` section.
2. Run `make release-prep VERSION=0.2.0`; review all manifest and lockfile diffs.
3. Run `make ci` and `make embedded-bun`.
4. Run `make host-release-check`, then `make host-release-bundle`; inspect the
   archive allowlist, checksums, target, version output, and unsigned status.
5. Run both package dry-runs (`bun pm pack --dry-run` in each package) and the
   package consumer smoke already covered by `make ci`.
6. Review the dated changelog, generated locks, archive name, and `git diff
   --check`; commit the version cut and release notes.
7. After the human approves the final commit, create the `v0.2.0` tag and push
   only through the separately approved release procedure. Publish npm packages
   only after registry/token/access decisions are confirmed.

The implementation backlog for the current 0.2.0 contract is empty. Remaining
items are documented platform acceptance boundaries or human release decisions:
unsigned/notarization, the future-incompatibility choice, display-backed
validation, cross-platform runner evidence, upstream TextInput/RTL/style/Image
gaps, and npm/crates.io publication policy. The cross-platform workflow is
scaffolded but unvalidated until push/PR runners execute; local macOS probes are
not runner proof. The human release owner controls the version/date decision,
artifact signing, tag creation, registry credentials, and publication.

This file is local evidence under `/.scratch/`, which is ignored by
`.gitignore` by default. The readiness report and this checklist are force-added
under the repository's evidence convention; neither is a published artifact. The
companion readiness report is the committed-tree-facing summary; its five-gate
matrix and this checklist's seven-step sequence are consistent.

No option is selected by this checklist.

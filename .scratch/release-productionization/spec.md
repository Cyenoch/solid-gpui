# Release productionization

This effort covers the process-runtime macOS ARM host candidate, package artifacts,
CI gates, and evidence required before any real publication. It does not publish
artifacts or claim that embedded Bun is a standalone release.

Known risks are tracked as separate triage issues under `issues/`.

# Publish-readiness audit

Generated 2026-08-30 against HEAD `064e4b0` plus the metadata changes in this working tree. This is read-only analysis with metadata-only fixes; no publish command was run and no live gallery process was touched.

## Domain A — npm package publish metadata

| Package | Description | Keywords | Repository | License | Engines | Files allowlist | Exports/types | Peer dependency | Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `@react-gpui/core` | React custom renderer for the native GPUI host. | `react`, `gpui`, `custom-renderer`, `native-ui`, `bun` | Added `https://github.com/Cyenoch/react-gpui.git` | Apache-2.0 | `bun >=1.4.0` | `dist`, `README.md`, `LICENSE` | `.` maps `types` to `dist/index.d.ts` and `import`/`default` to `dist/index.js`; `main` and `types` agree | `react ^19.2.0` | Ready |
| `@react-gpui/dev` | Fast Refresh transforms and runtime support for React GPUI development. | `react`, `gpui`, `fast-refresh`, `babel`, `bun`, `testing` | Added `https://github.com/Cyenoch/react-gpui.git` | Apache-2.0 | `bun >=1.4.0` | `dist`, `README.md`, `LICENSE` | `.` maps `types` to `dist/index.d.ts` and `import`/`default` to `dist/index.js`; `main` and `types` agree | `react ^19.2.0` | Ready |

`sideEffects` is intentionally not declared: the built modules import runtime dependencies and the dev transform installs global/Bun hooks when explicitly called, so claiming side-effect freedom would be too strong. Both package manifests already had complete descriptions, keywords, license, engines, allowlist, export maps, and peer ranges. The only metadata gap found was the repository URL, now fixed.

### Tarball content verdict

`bun pm pack --dry-run` was run in each package. Core produced exactly 18 files: `package.json`, `LICENSE`, `README.md`, `dist/index.js`, `dist/index.d.ts`, and the generated declaration modules under `dist/renderer/` plus `hooks`, `protocol`, `protocol-tap`, `style`, `surface-host`, and `transport`. Dev produced exactly 8 files: `package.json`, `LICENSE`, `README.md`, `dist/index.js`, `dist/index.d.ts`, `dist/fast-refresh.d.ts`, `dist/testing.d.ts`, and `dist/transform.d.ts`. No `src/`, tests, examples, or unrelated assets shipped. Core's `examples/assets/tuffy.ttf` and its license are development/example inputs and are not runtime dependencies of the package; no runtime asset is missing from the allowlist.

`make bun-pack-smoke` passed after the metadata change: both packages rebuilt, packed, installed from tarballs in an external consumer, and exercised through the 59-package consumer smoke, including the dev package's `file:` core dependency resolved from the packed core tarball.

## Domain B — crates.io metadata

The workspace and all three crates use Apache License 2.0, confirmed by the existing `LICENSE` opening (`Apache License, Version 2.0, January 2004`). The standard SPDX `license = "Apache-2.0"` is retained; redundant `license-file` was deliberately removed after Cargo warned that only one is necessary. The workspace now supplies description, keywords, categories, repository, and README defaults; each crate explicitly inherits repository/README and supplies unambiguous package-specific keywords/categories.

| Manifest | Description | Keywords | Categories | License | README | Repository | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Workspace `Cargo.toml` | `React GPUI renders React trees into one or more native GPUI surfaces.` | `react`, `gpui`, `native-ui` | `gui` | Apache-2.0 | `README.md` | `https://github.com/Cyenoch/react-gpui` | Fixed |
| `react-gpui` | Existing: `Validated snapshot protocol and native tree for the React GPUI host` | `react`, `gpui`, `renderer`, `native-ui` | `gui` | inherited | inherited root README | inherited | Fixed |
| `react-gpui-host` | Existing: `Native GPUI host for running React GPUI renderer entries.` | `react`, `gpui`, `native-ui`, `application-host` | `gui`, `command-line-utilities` | inherited | inherited root README | inherited | Fixed |
| `react-gpui-bun` | Existing: `In-process Bun and JavaScriptCore adapter for the React GPUI host.` | `react`, `gpui`, `bun`, `javascriptcore` | `gui` | inherited | inherited root README | inherited | Fixed |

`cargo metadata --no-deps` confirmed all three packages resolve `license = Apache-2.0`, repository `https://github.com/Cyenoch/react-gpui`, README `../../README.md`, descriptions, keywords, and categories. Cargo archive listing also showed README and LICENSE included for each package. No version or publish configuration was changed.

## Domain C — unsafe audit

The workspace crates are **not** zero-unsafe: `crates/react-gpui-bun/src/lib.rs` contains the embedded Bun/JSC FFI boundary. Hits are the `unsafe extern "C"` declaration and callbacks at lines 30, 38–40, 171–172, 452, 457, 464, 479, 485, 493, 509, 515, and 523. Every unsafe operation has an adjacent safety comment describing lifetime/thread ownership, Bun's immutable frame, context validity, or Bun-provided writable capacity. The audit made no code edits. The additional hit in `crates/react-gpui/tests/module_boundaries.rs:203` is a string literal used to detect public `pub unsafe fn` declarations, not unsafe executable code.

## Domain D — stress example

`packages/react-gpui/examples/stress.tsx` demonstrates a process-mode stress/soak surface: it increments React state every 10 ms, renders the current tick, periodically calls `root.getWindowSize()`, and provides a button to advance the tick manually; SIGTERM exits cleanly. It is a valid repository example and needs no edit.

## Gates

| Gate | Result |
| --- | --- |
| `make bun-pack-smoke` | PASS — packed rebuild, tarball allowlist checks, external install, runtime/type consumer smoke; 59 packages installed |
| `cargo check -p react-gpui --locked` | PASS — compile completed; Cargo emitted only the existing `block v0.1.6` future-incompatibility warning |
| `bun pm pack --dry-run` core/dev | PASS — 18/8 files as listed above |
| `cargo metadata --no-deps --format-version 1` | PASS — all metadata fields resolve as intended |

## Commit and tree state

Metadata-only fixes are ready to commit as `chore(meta): publish readiness`. The user-owned `AGENTS.md` remains modified and is intentionally not staged. The report itself is ignored by default and must be force-added. No `.scratch/release-rehearsal-2/**` path was touched. No publication or live gallery process was started.

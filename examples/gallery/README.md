# gallery

Direct Bun Gallery. Run `bun run dev` here or `bun run gallery` at the workspace root.

`src/application.tsx` and `src/gallery` own the shared application. The Vite Gallery imports this workspace package.

# gallery-vite

Vite + Bun Gallery. Run `bun run dev` here or `bun run gallery:vite` at the workspace root.

`bun run build` produces `dist/main.js`. All pages and application state are imported from `@solid-gpui/gallery/application`. See [hot reload](../../docs/hot-reload.md).
